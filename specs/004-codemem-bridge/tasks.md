---

description: "Task list for CodeMem 004 — the bridge: Claude Code's source for the map, and the green-compile trigger"
---

# Tasks: CodeMem 004 — The Bridge

**Input**: Design documents from `/specs/004-codemem-bridge/`

**Prerequisites**: plan.md (STOP 1 ruled 2026-09-15; the review rulings of the same day — CON1–CON4, COR1–COR3,
INC1–INC4, DUP1, TIM1 — applied; analyze pass 2 ruled 2026-09-15 — I1, I6, G1, U1, I2–I5, G2–G4, U2, U3, C1 applied, the LOW items carried), spec.md (clarified; three ruling sessions recorded), spike.md (both facts
proven), research.md (R41–R59), data-model.md, contracts/tools.md, contracts/cli-config-hook.md, quickstart.md —
all present. Constitution **v1.3.0** governs. 003's code and its 69 tests are the starting point (commit `b2e0168`;
68 passed / 1 skipped / 2.2 min). **Branch**: `004-codemem-bridge`, created from `b2e0168`; the v1.3.0 amendment
and the 004 artifacts are committed there before the first Red (the Architect's instruction, 2026-09-15).

**Tests**: REQUIRED. FR-342–FR-351 and Article II make every behaviour a Red-first fact against real SQLite, the
real compiled fixture, a real repository and the real executable: write → run → confirm Red *for the stated reason*
→ record it in the test file header → implement → confirm Green → record. Every guard carries a `' FIRE:` line.
Tests for this feature live in `tests/CodeMem.Tests/Bridge/` (`B01`–`B08`), two new guards in `Guards/`, five
support classes in `Support/`. **Order rule (CON2)**: no implementation slice lands before its behaviour test exists
and is observed Red; a skeleton (signatures that throw `NotImplementedException("<story>")`) may land only after the
fact it will satisfy is written, and a skeleton that fails to compile is a Red only from that moment. **Production
route (CON3)**: B01 drives every read tool through `BridgeProcess.CallTool` over stdio and `extract` through the same
route for one gate refusal and one real launch; `BridgeHost` serves the fast in-process facts and is never the
production-route evidence. **No second stop**: each Red is recorded before its implementation; the work stops only
for a Red not named in plan §Test design.

**Organization**: Phase 1 setup (two projects, fixtures, support, the two new gates), Phase 2 foundational (the B01
Red first; then the two Core doors, the registry read, the server skeleton), Phases 3–7 the five user stories in
priority order (US1 read surface · US2 `type_usages` · US3 `map_status` · US4 `extract` and the hook · US5 twins),
Phase 8 polish and the live steps. Inside a story: the Red battery, then the slices, then Green, then the FIREs.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: US1–US5 from spec.md
- Every path is repository-relative

## Standing rules for every task that creates or edits a `.vb` file

- Header block first (`' File:`, `' Project:`, `' Description:`, `' Author: RCH Automation LLC`, `' Created: <date>`); XML docs on every public declaration; one type per file; Option Strict/Explicit On, Infer Off (project-wide).
- **No SQL literal anywhere in `src/CodeMem.Bridge/` or `src/CodeMem.Bridging/`**; every query is a named method under `src/CodeMem.Core/Repositories/` (`Registry/` for the store). `New SqliteConnection` appears in `MapDatabase.vb` and `StoreDatabase.vb` only (v1.3.0).
- `CodeMem.Bridge` (exe) holds `Program.vb`, `BridgeCommandLine.vb` and `Mcp/BridgeServer.vb` only (CON1); everything else of the bridge lives in `CodeMem.Bridging` (class library).
- Every read tool opens the map once per call and reads inside one read transaction that ends after the response is serialised (CON4).
- Red/Green/FIRE lines go in the test file header; every FIRE injection is reverted before the task closes.
- Existing tests this feature changes (`SqlLocationGateTests`, `R01_ScopeRootTests`) are edited in place with a dated header line, after their named Red is recorded.
- The committed fixture under `tests/CodeMem.Tests/Fixtures/Sample/` is never edited; every mutation runs on a `FixtureCopy`. Live files under `C:\_DB\` are read-only in every task but the two operator steps that say otherwise.
- Nothing in `rchaudio-a11y\MemOS` is read for code; nothing there is changed (FR-349).

---

## Phase 1: Setup

**Purpose**: The two projects exist in the solution on the feature branch, the fixtures and support the facts need
are in place, and the two new review gates stand before any bridge code.

- [X] T001 Confirm the working tree is on branch `004-codemem-bridge` at the commit that carries `.specify/memory/constitution.md` v1.3.0 and `specs/004-codemem-bridge/` (both committed before this task), `git status` clean; `dotnet build CodeMem.sln` 0 errors / 0 warnings; `dotnet test CodeMem.sln` 68 passed / 1 skipped. Record the confirmation under "Build and test" in `specs/004-codemem-bridge/quickstart.md`
- [X] T002 Create the two projects (CON1, the Extractor/Extraction shape): `src/CodeMem.Bridging/CodeMem.Bridging.vbproj` (class library, `net8.0`, `RootNamespace` CodeMem.Bridging, `OptionStrict` On, `OptionExplicit` On, `OptionInfer` Off, `GenerateDocumentationFile` true, `<Version>0.1.0</Version>`; `ProjectReference` to `..\CodeMem.Core\CodeMem.Core.vbproj` and `..\CodeMem.Extraction\CodeMem.Extraction.vbproj`; `PackageReference` `ModelContextProtocol.Core` 1.4.0 and `LibGit2Sharp` 0.32.0) and `src/CodeMem.Bridge/CodeMem.Bridge.vbproj` (`OutputType` Exe, the same settings, `RootNamespace` CodeMem.Bridge; `ProjectReference` to `..\CodeMem.Bridging\CodeMem.Bridging.vbproj` and `..\CodeMem.Extractor\CodeMem.Extractor.vbproj`, the latter so the extractor lands beside the bridge; no package of its own); add both to `CodeMem.sln` under the `src` solution folder with `dotnet sln CodeMem.sln add <each> --solution-folder src`; create `src/CodeMem.Bridge/Program.vb` (wiring only: parse → `serve` | `extract` | `hook` | `--help`; every branch but `--help` throws `NotImplementedException` for now) and `src/CodeMem.Bridge/BridgeCommandLine.vb` (the grammar of contracts/cli-config-hook.md §1: `serve` default, `extract` with exactly one of `--solution-key`/`--repo-path`/`--stale` plus optional `--on-green-build`, `hook`, `--config <path>` on every entry, case-insensitive names, `Usage` text listing the three entries and the exit codes); add `<ProjectReference>`s to `..\..\src\CodeMem.Bridging\CodeMem.Bridging.vbproj` and `..\..\src\CodeMem.Bridge\CodeMem.Bridge.vbproj` and `<None Update="Fixtures/Hooks/*.json" CopyToOutputDirectory="PreserveNewest" />` to `tests/CodeMem.Tests/CodeMem.Tests.vbproj`; `dotnet build` 0/0; `ProjectFileGateTests` green (six project files)
- [X] T003 [P] Create `tests/CodeMem.Tests/Fixtures/Hooks/posttooluse-green.json` and `tests/CodeMem.Tests/Fixtures/Hooks/posttoolusefailure-red.json` with the two payloads of spike.md §Fact 2 verbatim (from `tests/CodeMem.Tests/Fixtures/Hooks/spike-hook.log`, the spike's log copied verbatim into the tree on 2026-09-15 — U3; its two `received` blocks are the payloads; session ids and paths kept — they are data), plus `tests/CodeMem.Tests/Fixtures/Hooks/posttooluse-cd-and-test.json`: the green payload with `tool_input.command` replaced by `cd C:\\Users\\rchau\\source\\repos\\GameRoom && dotnet test GameRoom.Tests\\GameRoom.Tests.vbproj --nologo` (the `cd`-prefix shape R48 parses)
- [X] T004 [P] Create `tests/CodeMem.Tests/Support/RegistryFixture.vb` (`IDisposable`): `New()` creates `<temp>\codemem-tests\store-<guid>.sqlite` holding exactly migration 029 §1's `code_map_solutions` table (the nine columns with their comments, `ix_code_map_solutions_project_id`, the partial unique index on `codemem_solution_id`), nothing else; `Seed(projectId As Long, solutionKey As String, codememSolutionId As Long?, state As String, extractionScope As String) As Long`; `Shared Function WithoutTable() As RegistryFixture` (a store with one unrelated table `not_a_registry`); `Path`; `Dispose` deletes the file. Its DDL and INSERT literals are test SQL: add `Support/RegistryFixture.vb` to the exclusion list in `tests/CodeMem.Tests/Guards/SqlLocationGateTests.vb` with a dated header line ("2026-09-15 (004): RegistryFixture holds the 029 §1 DDL, transcribed — a copy of a contract, not a reference")
- [X] T005 [P] Create `tests/CodeMem.Tests/Support/GitFixture.vb` (Shared helpers over LibGit2Sharp; add `<PackageReference Include="LibGit2Sharp" Version="0.32.0" />` to the test project only if `Imports LibGit2Sharp` does not resolve transitively — R39): `Init(directory) As Repository` (writes `.gitignore` with `bin/` and `obj/`), `CommitAll(repo, message) As String` (stage `*`, commit with a fixed `Signature`, return the sha), `Touch(directory, relativeFile, text)`, `CheckoutNewBranch(repo, name)`, `RemoveGitDirectory(directory)` (deletes `.git` after disposing the repository)
- [X] T006 [P] Create `tests/CodeMem.Tests/Support/BridgeProcess.vb`: spawns `dotnet "<AppContext.BaseDirectory>\CodeMem.Bridge.dll" <args>` (the `ExtractorProcess` shape: redirected stdio, no window, kill after a timeout); `Shared Function Serve(configPath) As BridgeProcess` (starts `serve --config <configPath>`), `Initialize()` (sends `initialize` with `protocolVersion` 2025-06-18 and `notifications/initialized`, returns the `serverInfo`), `ListTools() As List(Of String)` (names from `tools/list`) and `ListToolDescriptions() As Dictionary(Of String, String)` (name → description from the same reply — G4), `CallTool(name, argumentsJson) As ToolReply` (a record with `IsError` and the parsed `result` text as `JsonDocument`), `Close()` (closes stdin, waits, returns the exit code); `Shared Function RunOnce(args, stdin As String) As ProcessReply` (`ExitCode`, `StandardOutput`, `StandardError`, `Elapsed`) for the `extract` and `hook` entries. No bridge types are referenced (it must compile before the bridge has any)
- [X] T007 [P] Create `tests/CodeMem.Tests/Guards/MemOsReferenceGateTests.vb` (FR-343): over every `.vbproj` under `src/` and `tests/` except `Fixtures/`, collect the `Include` attribute of every `ProjectReference`, `PackageReference` and `Compile` element (parse as XML); assert the number of `ProjectReference` elements found is > 0 ("the scan is vacuous"); assert no `Include` value contains `MemOS` or `rchaudio-a11y` (OrdinalIgnoreCase), naming the file and value on failure. Run → green; FIRE: insert `<ProjectReference Include="..\..\..\rchaudio-a11y\MemOS\MemOS.Core\MemOS.Core.vbproj" />` into `src/CodeMem.Bridging/CodeMem.Bridging.vbproj` and run `dotnet test --no-build --filter FullyQualifiedName~MemOsReferenceGateTests` (no rebuild: a dangling reference would break the build, and the gate reads the file at run time) → red naming the file; revert → green; record the FIRE line
- [X] T008 [P] Create `tests/CodeMem.Tests/Guards/BridgeSqlGateTests.vb` (FR-303, FR-304) with four facts using `SqlLocationGateTests.SourceFiles` and `TripwireTests.Normalise`: (1) `TheBridgeProjectsHoldNoSqlLiteral` — 0 literals matching `\b(SELECT|INSERT|UPDATE|DELETE|CREATE|DROP|ALTER|ATTACH|PRAGMA)\b` under `src/CodeMem.Bridge/` and `src/CodeMem.Bridging/`; (2) `RegistryReadsNameOnlyTheRegistryTable` — over `src/CodeMem.Core/Repositories/Registry/*.vb`: ≥ 1 literal starting with `SELECT` (vacuous guard — **Red until T013**), 0 write keywords, no `sqlite_master` (INC1), and every token after `FROM` or `JOIN` equals `code_map_solutions`; (3) `CoreReadMethodsAreSelectOnly` — every literal in `src/CodeMem.Core/Repositories/*.vb` whose enclosing declaration is `Function Read…`/`Function Search…` begins with `SELECT` or `WITH`; (4) `TheStoreIsOpenedOnlyByTheBridge` — the identifiers `StoreDatabase` and `CodeMapSolutionsRepository` appear in no `.vb` file under `src/CodeMem.Extraction/` or `src/CodeMem.Extractor/`, and outside their own definitions only under `src/CodeMem.Bridging/`, `src/CodeMem.Bridge/` and `tests/` (the guard on v1.3.0's last sentence: the extractor never opens the store). Run → (2) red for the stated reason (no `Registry/` folder yet), (4) green (a guard on absence, trusted through its T013 fire); record the Red; the FIREs come at T013
- [X] T009 [P] Create `.mcp.json` at the repository root with exactly the text of contracts/cli-config-hook.md §3 (server `codemem`, `type` stdio, `command` `src/CodeMem.Bridge/bin/Release/net8.0/CodeMem.Bridge.exe`, `args` `["serve"]`)

**Checkpoint**: the solution builds with six projects; the two new gates run (one Red named); no bridge behaviour yet.

---

## Phase 2: Foundational — the B01 Red, the doors, the registry, the server skeleton

**Purpose**: The read-only map door with its read transaction, the store door, the registry read, and a server that
starts and lists tools — each behind a fact that was Red first (CON2).

**⚠️ CRITICAL**: No user story work until T016 records Green.

- [X] T010 Create `tests/CodeMem.Tests/Bridge/B01_ReadOnlyContractTests.vb` (FR-342; `<Collection("Fixture")>`) with six facts: (1) `ReadOnlyConnectionStringCarriesModeReadOnly` — `MapDatabase.ReadOnlyConnectionString(path)` contains `Mode=ReadOnly` and `Default Timeout=3`; (2) `AWriteThroughTheReadOnlyDoorIsRefused` — extract the committed fixture into a `TempMap`, `MapDatabase.OpenReadOnly(map.Path)`, `INSERT INTO map_identity` through `CreateCommand` → `SqliteException` with `SqliteErrorCode` 8; (3) `OpeningAMissingPathCreatesNothing` — `OpenReadOnly` of `<temp>\absent-<guid>.sqlite` throws `SqliteException` code 14 and the file does not exist afterwards; (4) `EveryReadToolLeavesTheMapByteIdenticalOverStdio` (CON3) — hash the fixture map (`MapSnapshot.FileBytesHash`); `BridgeProcess.Serve(config)`; through `CallTool` call every name `ListTools()` returns except `extract`, with the arguments a table in the test supplies per name (`solutionKey "Sample"`, `symbolId` = the id of `T:Sample.Consumer`, `name "Widget"`), asserting each reply is not an error, the hash is unchanged after each, and `> 0` tools were called (vacuous guard); `Close()` exits 0; (5) `TheExecutableServesOverStdio` — `Initialize()` returns `serverInfo.name = "codemem"`, `ListTools()` equals `BridgeTools.RegisteredToolNames`, every description `ListToolDescriptions()` returns equals that tool's `BridgeToolDescriptions` constant (FR-312 on the production route — G4), no `-journal` beside the map after the session; (6) `APublicationCannotStraddleAToolsReads` (CON4) — `BridgeHost` with `ReadSeams.AfterScopeResolved` set to an action that, on a second read-write connection with a 1 s busy timeout (`MapQueries.TryRenameSolution(db, id, "changed", busySeconds:=1)` — a new helper in the allowed test SQL file), attempts an UPDATE and records the `SqliteException` code; call `solutions`; assert the recorded code is 5 (busy: the tool's read transaction holds the map until its response is serialised), the response carries the original name, and the same UPDATE succeeds after the call. Header: "RED: <date> — `OpenReadOnly`, `ReadOnlyConnectionString`, `BeginRead`, `BridgeTools`, `BridgeHost`, `ReadSeams` do not exist (compile failure recorded as the Red now that the facts are written; the slices land in T011–T014)"
- [X] T011 First, T010's Red recorded, land the skeleton the B01 facts reference (CON2 permits a skeleton once its fact exists — I1, analyze pass 2): `src/CodeMem.Bridging/Mcp/ReadSeams.vb` (test-only seam, `AfterScopeResolved As Action`, Nothing in production — the `RunSeams` shape), `src/CodeMem.Bridging/Mcp/BridgeTools.vb` (`Public Class BridgeTools`; `New(configPath As String, launcher As Object, Optional seams As ReadSeams = Nothing)` — the launcher parameter is typed `Object` until T037 lands the seam, then retyped; `Shared ReadOnly Property RegisteredToolNames As String()` (empty until each story registers its tool); eight public methods with the signatures of contracts/tools.md §1 (`Optional … As Long? = Nothing`, `Optional … As String = Nothing`, `Optional stale As Boolean? = Nothing`), each returning `CallToolResult`, each throwing `NotImplementedException("<story>")` for now; the private `Refuse`/`Answer` helpers arrive with T022) and `tests/CodeMem.Tests/Support/BridgeHost.vb` (`New(configPath, Optional seams As ReadSeams = Nothing)` with a `ScriptedLauncher`-typed field added at T037; `Call(name, args As IDictionary(Of String, Object)) As (IsError As Boolean, Json As JsonDocument, Text As String)` dispatching to the eight methods; `WriteConfig(mapPath, storePath, extractorPath, enabled, onGreenBuild) As String` writing a temp config file); `dotnet build` → 0/0 — the assembly compiles again and every fact from here on can run. Then in `src/CodeMem.Core/Repositories/MapDatabase.vb` add `Public Shared Function ReadOnlyConnectionString(path As String) As String` (a `SqliteConnectionStringBuilder` with `DataSource = path`, `Mode = SqliteOpenMode.ReadOnly`, `Pooling = False`, `DefaultTimeout = 3`), `Public Shared Function OpenReadOnly(path As String) As MapDatabase` (opens with that string; **no** `journal_mode` pragma, no `foreign_keys` pragma; on any exception disposes the connection and rethrows), `Public Sub BeginRead()` (an ordinary `BEGIN` — deferred; sets `_inTransaction`) and `Public Sub EndRead()` (`COMMIT`; a no-op when none is open) (CON4). Header gains a dated 004 line; XML docs cite R44, R58 and FR-303. Build; run B01 facts (1)–(3) → green; FIRE: change `Mode` to `SqliteOpenMode.ReadWrite` → (2) red (the INSERT succeeds) and (3) red (a file appears); revert → green; record
- [X] T012 Create `src/CodeMem.Core/Repositories/StoreDatabase.vb` (`Public Class StoreDatabase`, `IDisposable`; `Shared Function OpenReadOnly(path As String) As StoreDatabase` with the same read-only builder shape; `CreateCommand()`; nothing else — no table probe, INC1). Build; run `dotnet test --filter FullyQualifiedName~SqlLocationGateTests` → **the named Red**: `OnlyMapDatabaseOpensAConnection` expects `{MapDatabase.vb}`, finds two; record the Red line in `tests/CodeMem.Tests/Guards/SqlLocationGateTests.vb`'s header, then amend the assertion in place to `New String() {"MapDatabase.vb", "StoreDatabase.vb"}` (sorted) with the dated line citing constitution v1.3.0; re-run → green; FIRE: add `Dim probe As SqliteConnection = New SqliteConnection("Data Source=:memory:")` to `src/CodeMem.Bridge/Program.vb` → red naming `Program.vb`; revert → green; record
- [X] T013 Create `src/CodeMem.Core/Records/RegistryRecord.vb` (`Id`, `ProjectId`, `SolutionKey`, `CodememSolutionId As Long?`, `ExtractionScope`, `State`, `Notes`), `src/CodeMem.Core/Repositories/Registry/RegistryTableMissingException.vb` (Core's typed signal that the store holds no `code_map_solutions`) and `src/CodeMem.Core/Repositories/Registry/CodeMapSolutionsRepository.vb` (`Public Module`; `ReadAll(store As StoreDatabase) As List(Of RegistryRecord)` — `SELECT id, project_id, solution_key, codemem_solution_id, extraction_scope, state, notes FROM code_map_solutions ORDER BY solution_key`; a `SqliteException` whose `SqliteErrorCode` is 1 and whose message contains `no such table` is translated to `RegistryTableMissingException`; no `sqlite_master` query — INC1). Run `BridgeSqlGateTests` → (2) green; FIRE (2): add a literal `"SELECT id FROM projects"` to the module → red ("FROM projects"); revert; FIRE (1): add `"SELECT 1"` to `src/CodeMem.Bridge/BridgeCommandLine.vb` → red; revert; FIRE (4): add `Dim probe As StoreDatabase = Nothing` to `src/CodeMem.Extraction/Run/ExtractionRun.vb` → red naming the file; revert → green; record all three
- [X] T014 Create the server behind B01's Red (`BridgeTools`, `ReadSeams` and `BridgeHost` landed as T011's skeleton — I1): in `CodeMem.Bridging` — `src/CodeMem.Bridging/Mcp/BridgeToolDescriptions.vb` (`Public Module` of eight `Const` strings — the texts of contracts/tools.md §2, verbatim); in `CodeMem.Bridge` — `src/CodeMem.Bridge/Mcp/BridgeServer.vb` (`Shared Function RunAsync(configPath) As Task(Of Integer)`: `McpServerOptions` with `ServerInfo` name `codemem` version from the assembly, `ToolCollection` built by `McpServerTool.Create(delegate, New McpServerToolCreateOptions With {.Name, .Description, .ReadOnly = True except extract})` for every name in `RegisteredToolNames`; `New StdioServerTransport("codemem", Nothing)`; `McpServer.Create(transport, options, Nothing, Nothing)`; `Await RunAsync`; `Await DisposeAsync` — never inside `Finally`), and wire `Program.vb`'s `serve` branch to it
- [X] T015 `dotnet build CodeMem.sln` → 0/0. Run `dotnet test --filter "FullyQualifiedName~B01_|FullyQualifiedName~BridgeSqlGate|FullyQualifiedName~SqlLocationGate|FullyQualifiedName~MemOsReference"` → B01 (1)–(3) green, (4) **red for the stated reason** (0 tools listed: the vacuous guard fires — record), (5) green through `BridgeProcess` (the server starts, lists 0 tools, exits 0), (6) red on `NotImplementedException("US1")`; record every line in the B01 header
- [X] T016 Run the whole suite → every 001–003 test green (the amended `SqlLocationGateTests` included); record the run line and the count in `specs/004-codemem-bridge/quickstart.md` "Build and test"

**Checkpoint**: the doors and the registry read exist and are tested; the server starts from the test output
directory; no tool answers yet; every foundation slice landed behind a Red.

---

## Phase 3: User Story 1 — Claude Code asks the map while the Shell is closed (Priority: P1) 🎯 MVP

**Goal**: The five ported readers with the 056/058/060 shapes, the same scope and identity rules, the same
caveats, the same refusals by name; every call inside one read transaction; every call leaves the map byte-identical.

**Independent Test**: B02 green on fixture maps; B01 (4) green over the five tools through the executable; the
live read facts of B08 part 1 when armed.

- [X] T017 [US1] Create `tests/CodeMem.Tests/Bridge/B02_PortedReaderTests.vb` (`<Collection("Fixture")>`, fixture map extracted once per class into a `TempMap`, a `RegistryFixture` seeded `(131373, "Sample", <the map's solution id>, "active", <the .sln path>)`; fast facts through `BridgeHost` — the production route is B01 (4)): (1) `SolutionsHasTheMapReadShape` — every property of 056 §1.1 present (`map.guid` = `MapQueries.ReadMapGuid`, `schemaVersion` 2, one solution `Sample` with `latestRun` non-null and the ten counts equal to `MapQueries.ReadRuns`' last row, `dirtyState` `unknown` exactly when `commitSha` is null); (1b) `SolutionsReportsTheLatestRunOfAnyOutcome` (INC4) — on a copy whose newest run is a `failed` row (the residual-mismatch route of I8's fixture writes one), `latestRun.outcome` is `failed` and `runId` is the newest; (2) `SymbolSearchByKeyAndByProjectAgree` — `symbol_search(solutionKey "Sample", name "Widget")` and `symbol_search(projectId 131373, name "Widget")` return the same rows (`id`, `docCommentId`, `kind`, `name`, `container`, `project`, `path`, `line`), `total` equal, `truncated` False; by project the four registry counts are numbers and `hadNothingToSearch` False; by key they are null; a `projectSymbolId` narrows rows and not `total`; (3) `SymbolDetailShowsPartsAndEightVerbs` — `symbol_detail` of `T:Sample.MainForm` (a partial type): 2 parts, the one containing the name flagged; both `outbound` and `inbound` carry all eight keys; an outbound `extends` to `System.Windows.Forms.Form` has `targetSymbolId` null, `external` True, `targetDocCommentId` present; (4) `ReferencesAreExactIdentityWithoutContainment` — `references` of `P:Sample.MainForm.Timer1` includes the three bare-name occurrences 003 recorded (`AddHandler` line, `Timer1.Start()`, `Timer1.Stop()`) at their lines, no occurrence's verb is `part_of`, and `count` = `occurrences.length` — the total is not pinned, since Stage A's member-access rule also records the Designer's `Me.Timer1` sites; (5) `OrphansHaveTheSixtyShape` — `orphans(solutionKey "Sample")`: `filters`, `notExamined` = `["namespace","project"]`, `byProject` lists both fixture projects, `byKind` twelve keys with zeros, `total` = `orphans.length`; `orphans(kind "project")` → `KindNotExamined`; `orphans(projectSymbolId <a class id>)` → `NotAProjectRow`; (6) `EveryOccurrenceCarriesAnIdOrIsExternal` (FR-348) — create `tests/CodeMem.Tests/Support/OccurrenceAssertions.vb` with `Shared Sub AssertIdentity(occurrence As JsonElement)` (the target's id non-null, or `external` True with a non-empty doc-comment id; never neither) and apply it to every occurrence returned by (3) and (4); B03 and B07 apply the same helper; (7) `RefusalsComeInOrderAndByName` — a table of cases asserting `IsError` and the distinguishing phrase of contracts/tools.md §6: no scope **and** a config file absent → `exactly one of projectId` (arguments before configuration, INC2); config file absent → `not configured`; `mapPath` absent → `No CodeMem map exists` and the path still absent; `MapQueries.CreateForeignDatabase` → `not a CodeMem map`; `V1MapFixture` → `upgrades the map in place`; `MapQueries.SetSchemaVersion(copy, 3)` → `newer contract`; `RegistryFixture.WithoutTable` with `projectId` → `no code_map_solutions table`; both scopes → `Both`; `symbol_search` with no filter → `name, a kind, or both`; `kind "widget"` → the fourteen kinds listed; `solutionKey "Nope"` → `holds no solution with key`; `symbolId 999999` → `holds no symbol with id`; a retired id (re-extract a copy after deleting `Sample.Lib/Twins.vb`, take the retired id) → `retired`; an id from a second solution in the same map (`FixtureCopy` extracted with `--solution-key Other`) asked under `Sample` → `not in the requested scope`; `projectId 424242` with no row → not an error, `hadNothingToSearch` True, reason names project 424242; (8) `DescriptionsCarryTheCaveats` (FR-312) — every phrase of contracts/tools.md §2 in bold, per tool, present in `BridgeToolDescriptions`. Run → every fact red on `NotImplementedException("US1")` (the named Red); record
- [X] T018 [US1] Add the read methods every story shares, with their records: `src/CodeMem.Core/Records/RunRecord.vb` (every `extract_runs` column, nullable where the schema is), `src/CodeMem.Core/Records/MapIdentityRecord.vb` (`MapGuid`, `SchemaVersion`, `CreatedUtc`); in `src/CodeMem.Core/Repositories/MapIdentityRepository.vb` `ReadIdentity(db) As MapIdentityRecord`; in `SolutionsRepository.vb` `ReadAll(db) As List(Of SolutionRecord)` (ordered by key), `ReadById(db, id) As SolutionRecord` (Nothing when absent), `ReadByKey(db, key) As SolutionRecord`; in `ExtractRunsRepository.vb` `ReadLatest(db, solutionId) As RunRecord` (highest id, **any outcome** — what `solutions` reports, INC4), `ReadLatestCompleted(db, solutionId) As RunRecord` (what `map_status` and `extract` use), `ReadById(db, runId) As RunRecord`. Every literal SELECT-only (BridgeSqlGate (3) green); the tripwire green; dated header lines
- [X] T019 [US1] Create the bridge's foundation in `CodeMem.Bridging`, one type per file: `src/CodeMem.Bridging/Configuration/BridgeConfig.vb` (`MapPath`, `StorePath`, `ExtractorPath` nullable, `ExtractEnabled` and `ExtractOnGreenBuild` default False, `SourcePath`); `Configuration/BridgeConfigFile.vb` (`Shared Function DefaultPath() As String` = `bridge.config.json` beside `AppContext.BaseDirectory`; `Shared Function Load(path As String) As BridgeConfig` — `System.Text.Json`, unknown keys ignored, missing file / not JSON / missing `mapPath` or `storePath` throw `BridgeRefusalException(Unconfigured, key, path)`; `extract.enabled`/`extract.onGreenBuild` absent → False); `Refusals/BridgeRefusalKind.vb` (the closed enum of data-model §10, in that order — `AmbiguousTarget` and `ChildTimedOut` included); `Refusals/BridgeRefusal.vb` (`Kind`, `Text`; `Shared Function For(kind, facts As IDictionary(Of String, String)) As BridgeRefusal` — the one wording owner, texts of contracts/tools.md §6); `Refusals/BridgeRefusalException.vb`; `Reading/BridgeSchemaPin.vb` (`Public Const Required As Integer = 2` — **not** `SchemaVersion.Current`); `Reading/MapAccess.vb` (`Shared Function OpenRead(config) As MapDatabase`: absent path → `MapAbsent`; `OpenReadOnly`; `InspectSchema`: `Fresh`/`Foreign` → `NotAMap`, version < 2 → `VersionBelow`, > 2 → `VersionAbove`; then `BeginRead()` — one open, one read transaction per call (CON4); `SqliteException` code 5 → `Busy` naming 3 seconds, any other → `Unopenable` with the driver text); `Reading/StoreAccess.vb` (`Shared Function Open(config) As StoreDatabase`: absent/unopenable → `Unopenable` naming the store path; `RegistryTableMissingException` from the repository → `RegistryAbsent` — no table probe, INC1); `Reading/Envelopes/BridgeJson.vb` (one `JsonSerializerOptions`: camelCase, `DefaultIgnoreCondition.Never`, not indented); `Reading/Envelopes/ScopeEnvelope.vb` (058 §3.1 fields); `Reading/ScopeResolver.vb` (`Resolve(projectId As Long?, solutionKey As String, config, map As MapDatabase) As ResolvedScope` — takes the call's open map (CON4): neither → `ScopeMissing`, both → `ScopeConflict`; by key → the map's `ReadByKey` else `SolutionKeyUnknown`, registry fields Nothing; by project → `StoreAccess`, rows with that `project_id`: active + bound + map row exists → included, the four counts and `danglingSolutionIds` filled, `hadNothingToSearch` True with the reason "no registry row names project N" or "every row for project N is unbound or inactive")
- [X] T020 [US1] Add the Core read methods and records the readers need: `src/CodeMem.Core/Records/SymbolRecord.vb` (every `code_symbols` column plus `ContainerName`, `ProjectName` joined), `Records/PartRecord.vb`, `Records/EdgeRecord.vb` (every `code_edges` column plus source and target `name`/`kind` joined, `ViaName`), `Records/OrphanRecord.vb`; in `Repositories/CodeSymbolsRepository.vb`: `ReadById(db, id) As SymbolRecord` (active or retired; the caller decides), `Search(db, solutionIds, name, kind, projectSymbolId) As List(Of SymbolRecord)` — **raw rows, uncapped** (COR3): `is_active = 1`, `name` matched with `instr(lower(name), lower(@name)) > 0` — no `LIKE`, ordered `name, kind, path, start_line, project_symbol_id, id` (the folder groups and caps in memory); `ReadProjects(db, solutionIds) As List(Of SymbolRecord)`, `ReadOrphans(db, solutionIds, kind, projectSymbolId) As List(Of OrphanRecord)` — **written fresh from the semantics the description states** (an active symbol of an examined kind — the fourteen less `namespace` and `project` — with no inbound edge except `part_of` whose source lies outside the symbol's containment subtree, where an inbound edge to any member counts for the container; a `handles` target and a member that implements an interface member excluded; ordered as 059 §3.1), never transcribed from MemOS; in `CodeEdgesRepository.vb`: `ReadOutbound(db, symbolId)`, `ReadInbound(db, symbolId)`, `ReadReferences(db, symbolId)` (`target_symbol_id = @id AND verb <> 'part_of'`, ordered `path, start_line, start_column`); in `CodePartsRepository.vb`: `ReadParts(db, symbolId)`. Every literal SELECT-only; dated header lines; `BridgeSqlGateTests` (3) and the tripwire green
- [X] T021 [US1] Create the envelopes and readers in `CodeMem.Bridging`: `src/CodeMem.Bridging/Reading/Envelopes/SymbolRefEnvelope.vb`, `DeclarationEnvelope.vb` (with `compiledInto`), `OccurrenceEnvelope.vb` (data-model §5), `SolutionsEnvelope.vb`, `SymbolSearchEnvelope.vb`, `SymbolDetailEnvelope.vb`, `ReferencesEnvelope.vb`, `OrphansEnvelope.vb`; `src/CodeMem.Bridging/Reading/TwinFolder.vb` as a pass-through for now (`Fold(rows) As List(Of DeclarationEnvelope)`: one declaration per row, `compiledInto` of one — the fold arrives in US5, COR3); `src/CodeMem.Bridging/Reading/Readers/SolutionsReader.vb` (`ReadLatest`, any outcome — INC4), `SymbolSearchReader.vb` (`Search` → `TwinFolder.Fold` → `total` = declarations, cap 200 → `truncated`), `SymbolDetailReader.vb` (parts with `containsDeclaringLocation`; outbound/inbound grouped under the eight verbs in the fixed order, external marking, `via`), `ReferencesReader.vb`, `OrphansReader.vb` (`notExamined`, `byProject` for every active project in scope with zeros, `byKind` for the twelve examined kinds, `filters`); each reader takes the call's `MapDatabase`, a `ResolvedScope` and its arguments and returns its envelope; the symbol-level refusals (`SymbolNotFound` → `SymbolOutOfScope` → `SymbolRetired`, `KindNotExamined`, `NotAProjectRow`) raised in one private helper the three id-taking readers share
- [X] T022 [US1] In `src/CodeMem.Bridging/Mcp/BridgeTools.vb` add the private helpers `Refuse(BridgeRefusal) As CallToolResult` (`IsError = True`, one `TextContentBlock`) and `Answer(json) As CallToolResult`, then implement `Solutions`, `SymbolSearch`, `SymbolDetail`, `References`, `Orphans` in this shape (CON4, INC2): argument refusals first → `BridgeConfigFile.Load` → `Using map As MapDatabase = MapAccess.OpenRead(config)` → `ScopeResolver.Resolve(…, map)` → `seams?.AfterScopeResolved` → the reader → `BridgeJson` serialise → `map.EndRead()` → `Answer`; every `BridgeRefusalException` → `Refuse`; the order of contracts/tools.md §4; register the five names in `RegisteredToolNames`; `BridgeServer` registers them with `ReadOnly = True`
- [X] T023 [US1] Build; run `--filter "FullyQualifiedName~B01_|FullyQualifiedName~B02_"` → B02 (1)–(8) green; B01 (4) green over five tools through the executable, (5) lists five, (6) green (busy code 5 during the call; the original name in the response; the rename succeeds afterwards); record GREEN lines with the date in the headers of `tests/CodeMem.Tests/Bridge/B01_ReadOnlyContractTests.vb` and `tests/CodeMem.Tests/Bridge/B02_PortedReaderTests.vb`
- [X] T024 [US1] FIREs, each recorded: (a) in `src/CodeMem.Bridging/Mcp/BridgeToolDescriptions.vb` remove the phrase `Containment (part_of) is not a reference` → B02 (8) red; revert; (b) in `src/CodeMem.Core/Repositories/CodeSymbolsRepository.vb` drop `AND is_active = 1` from `Search` → B02 (7)'s retired case red; revert; (c) in `src/CodeMem.Core/Repositories/CodeEdgesRepository.vb` make `ReadReferences` include `part_of` → B02 (4) red; revert; (d) in `src/CodeMem.Bridging/Mcp/BridgeTools.vb` call `map.EndRead()` before the reader instead of after serialisation → B01 (6) red (the rename succeeds mid-call); revert → green
- [X] T025 [US1] Create `tests/CodeMem.Tests/Bridge/B08_LiveMapTests.vb` (`SkippableFact`s; `Skip.If` when `CODEMEM_LIVE_MAP` or `CODEMEM_LIVE_STORE` is unset; a config file written to a temp path with both gates off; **read-only**): (1) `LiveSolutionsListsThree` — keys GameRoom, CodeMem, MemOS with runs 4, 5, 6 (or later); (2) `LiveReferencesOnShoeDrawAreSeven` — `references(solutionKey "GameRoom", symbolId 1200)` count 7, all `calls`, all in `GameRoom/Games/Blackjack/BlackjackControl.vb`; (3) `LiveOrphansGameRoomIsTwoHundredSeventyNine` — `orphans(solutionKey "GameRoom")` total 279 with `byProject` 73 + 206 (the 003 figures — an exact assertion: a different number is a Red to diagnose before anything else, and only the Architect accepts a changed number); (4) `LiveSearchByProjectResolvesThroughTheRegistry` — `symbol_search(projectId 132040, name "btnDeal_Click")` one row, id 584; each call timed with a `Stopwatch` around `BridgeHost.Call` and asserted under 3 s (SC-301), the times printed to the test output; hash of the live map equal before and after the four calls. Run armed once on this machine; record the observed values in `specs/004-codemem-bridge/quickstart.md` "Record"
- [X] T026 [US1] Run the whole suite unarmed → green (B08 Skipped); record the run line in `specs/004-codemem-bridge/quickstart.md`

**Checkpoint**: the MVP — Claude Code can ask the map through the bridge; the map is untouched.

---

## Phase 4: User Story 2 — "Who uses this class" is one call (Priority: P2)

**Goal**: `type_usages` on a type kind: the union of the finding, deduplicated by edge id, grouped by verb,
`fromInside` flagged, `total` and `fromOutside`; `references` unchanged.

**Independent Test**: B03 green on a fixture copy; B08 (5) on the live MemOS map when armed.

- [X] T027 [US2] Create `tests/CodeMem.Tests/Bridge/B03_TypeUsagesTests.vb` on a `FixtureCopy`: `Replace("Sample.Lib/Widgets.vb", "            Return ""Alpha:"" & input", "            Return ""Alpha:"" & input" & vbLf & "        End Function" & vbLf & "        Public Sub New()" & vbLf & "        End Sub" & vbLf & "        Public Function Twice(input As String) As String" & vbLf & "            Return Serve(input) & Serve(input)")` (the anchor is the one line unique to `AlphaService`, inside `Serve` after the `Implements` clause — an `Implements` statement must directly follow the class statement, so nothing is inserted before it; the file uses LF line endings and `Replace` requires a unique anchor; the original `End Function` then closes `Twice`) (an explicit constructor so `New AlphaService()` targets a row, and a sibling calling `Serve` by bare name — an inside use); write `Sample.App/ServiceUser.vb` (`Public Class ServiceUser` with `Public Function Run() As String` doing `Dim s As AlphaService = New AlphaService()` and `Return s.Serve("x")` — a constructor call and a member call from the other project); extract into a `TempMap`. Facts: (1) `TypeUsagesUnionsConstructorsMembersAndImplements` — `type_usages` of `T:Sample.Widgets.AlphaService`: the constructor call from `ServiceUser.Run` (target role `constructor`, `fromInside` False), the `Serve` call from `ServiceUser.Run` (role `member`, `fromInside` False), the two `Serve` calls from `Twice` (`fromInside` True), and the `uses` occurrence from `ServiceUser.Run`'s `As AlphaService` clause (role `type`, `fromInside` False — an `As` clause binds to the type and the uses rule records it, 003 R34); `byVerb` has all seven keys; `total` = occurrences count; `fromOutside` = count of `fromInside` False; every occurrence passes `OccurrenceAssertions.AssertIdentity` (FR-348); (1b) `ImplementsIsAnInboundUseOfTheInterface` — `type_usages` of `T:Sample.Widgets.ISampleService`: `byVerb.implements` ≥ 2 with `AlphaService` and `BetaService` among the sources (role `type`), `byVerb.calls` 0; (1c) `EveryEdgeAppearsExactlyOnce` (DUP1) — over `type_usages` of `AlphaService`, `ISampleService` and `MidWidget`, each occurrence carries the map's edge id and no id repeats; the `implements`/`extends` edges that both the "targets the type" arm and the "implements/extends" arm select appear once; (2) `ReferencesOnTheTypeStaysNarrow` — `references(AlphaService)` returns the `uses` occurrence at the `As AlphaService` clause and no `calls` occurrence (the constructor call targets the constructor, not the type); (3) `ExtendsIsAnInboundUse` — `type_usages(T:Sample.Widgets.MidWidget)` includes the `extends` occurrence from `LeafWidget`; (4) `NonTypesAreRefused` — `type_usages` of `M:Sample.Consumer.Build` → `not a type` and `Use references`; (5) `TheTypeUsagesDescriptionNamesTheConstructorGap` — `calls to each of its constructors`, `from inside`, `total`, `fromOutside` present in the description. Run → red on `NotImplementedException("US2")`; record
- [X] T028 [US2] In `src/CodeMem.Core/Repositories/CodeEdgesRepository.vb` add `ReadTypeUsages(db, typeId) As List(Of EdgeRecord)` — one statement per R51: `WITH RECURSIVE inside(id) AS (SELECT @type UNION ALL SELECT s.id FROM code_symbols s JOIN inside i ON s.container_id = i.id WHERE s.is_active = 1)` then a **`UNION` (not `UNION ALL`) on the edge id** of (a) edges with `target_symbol_id = @type AND verb <> 'part_of'`, (b) `calls` edges whose target is an active `constructor` with `container_id = @type`, (c) `calls`/`uses` edges whose target is an active non-constructor with `container_id = @type` (DUP1: (a) already holds the `implements`/`extends` arm, and the dedup makes any overlap harmless); each row joined to source and target symbols and carrying `source_symbol_id IN inside` as `from_inside`; ordered by the verb's fixed rank, `path`, `start_line`, `start_column`; `EdgeRecord` gains `FromInside As Boolean` and `TargetRole` (`type` | `constructor` | `member`, computed in SQL from the target's kind and id)
- [X] T029 [US2] Create `src/CodeMem.Bridging/Reading/Envelopes/TypeUsagesEnvelope.vb` (each occurrence carries `edgeId`) and `src/CodeMem.Bridging/Reading/Readers/TypeUsagesReader.vb` (kind check → `NotAType` for anything but `class, module, structure, interface, enum, delegate`; `byVerb` over the seven non-`part_of` verbs with zeros; `total`, `fromOutside`); implement `BridgeTools.TypeUsages` in the T022 shape; register `type_usages` (`ReadOnly = True`)
- [X] T030 [US2] Build; run `--filter "FullyQualifiedName~B03_|FullyQualifiedName~B01_"` → B03 (1)–(5) green, B01 (4) green over six tools; FIRE (a): count every occurrence as outside (`fromInside` always False) → (1) red on `fromOutside`; revert; FIRE (b): `UNION` → `UNION ALL` in `ReadTypeUsages` → (1c) red (a repeated edge id); revert → green; record in the header of `tests/CodeMem.Tests/Bridge/B03_TypeUsagesTests.vb`
- [X] T031 [US2] Add to `tests/CodeMem.Tests/Bridge/B08_LiveMapTests.vb` (5) `LiveTypeUsagesOnCodeMemMapReader` — `type_usages(solutionKey "MemOS", symbolId 3023)`: ≥ 16 occurrences with `target.id` 6661 and role `constructor`, `references(3023)` count 0, `total` ≥ 20, `fromOutside` ≥ 16 (SC-303), no repeated `edgeId`; the call timed with a `Stopwatch` around `BridgeHost.Call` and asserted under 3 s (SC-301 — G3), the time printed. Run armed once; record the values and the time in `specs/004-codemem-bridge/quickstart.md`

**Checkpoint**: US1 and US2 independently green.

---

## Phase 5: User Story 3 — The map says whether it is current (Priority: P2)

**Goal**: `map_status` per bound registry row with one verdict by name from the ruled set, every fact reported,
nothing guessed; unbound and inactive rows listed; the latest **completed** run is the one compared (INC4).

**Independent Test**: B04 green on a repository fixture; B08 (6) on the live map when armed.

- [X] T032 [US3] Create `tests/CodeMem.Tests/Bridge/B04_MapStatusTests.vb`: a `FixtureCopy` with `GitFixture.Init(copy.ParentDirectory)`, first commit `c1`, extracted into a `TempMap` with `--solution-key Sample`; a `RegistryFixture` seeded `(1, "Sample", <map id>, "active", …)`, `(2, "Unbound", Nothing, "active", …)`, `(3, "Retired", 77, "inactive", …)`, `(4, "Ghost", 99, "active", …)`. Facts: (1) `CurrentWhenHeadEqualsTheRunAndTheTreeIsClean` — verdict `current`, `head.sha` = `c1`, `behindBy` 0, `treeDirty` False; (2) `BehindCountsCommitsAndNamesHead` — `GitFixture.Touch` + commit `c2` → `behind`, `behindBy` 1, `head.sha` = `c2`, reason contains the short shas; (3) `DirtyWinsOverBehind` — an uncommitted edit on top of (2) → `dirty`, `behindBy` still 1 reported; (4) `DivergedNamesBothShasAndClaimsNoCount` — from `c1`, `CheckoutNewBranch("side")`, commit `c3` → `diverged`, `behindBy` null, reason names `c1` and `c3`; (5) `NoGitWhenTheRootIsNotARepository` — `RemoveGitDirectory` → `no_git` with the reason; a root that is a subdirectory of a repository (register a map row whose `repo_root` is `<parent>\Sample.Lib\`) → `no_git` "not a repository working directory"; (6) `MapMissingSolutionUnboundAndInactiveAreListed` — Ghost → `map_missing_solution` naming key and id 99; `unbound` holds `Unbound`, `inactive` holds `Retired` with its state; `bound` = 2 (Sample, Ghost); (7) `NoCommitRecordedIsNoGit` — a second `FixtureCopy` in the temp directory with no `GitFixture.Init` (the F04 shape; the committed fixture itself lies inside this repository and `GitProvenance` would record its HEAD — I2), extracted into its own `TempMap` and bound by a fifth registry row → `no_git` with reason "no commit recorded"; (7b) `TheLatestCompletedRunIsCompared` (INC4) — on the copy of (1b)'s shape (a `failed` row newer than the completed one) the entry's `run.runId` is the completed one and the verdict is computed from it; (8) `TheDescriptionNamesTheSixVerdicts` — `current`, `behind`, `dirty`, `no_git`, `map_missing_solution`, `diverged` and `one verdict by name` in the description; (9) `AnEmptyRegistryIsAStateNotARefusal` — a `RegistryFixture` with no rows: no refusal, `entries` empty, `bound` 0, `unbound` and `inactive` empty. Run → red on `NotImplementedException("US3")`; record
- [X] T033 [US3] Create `src/CodeMem.Bridging/Status/RepositoryFacts.vb` (LibGit2Sharp; `Shared Function Read(repoRoot As String, recordedSha As String) As RepositoryFacts` per R46: `Repository.Discover` → Nothing or a working directory ≠ the root (compared as `SolutionScope`-normalised full paths) → `Available = False` with the note; `Head.Tip` Nothing → note "no commit"; `RetrieveStatus().IsDirty`; `Lookup(Of Commit)(recordedSha)` Nothing → `RecordedFound = False`; `ObjectDatabase.FindMergeBase(recorded, head)` = recorded → `IsAncestor`, `BehindBy` = `Commits.QueryBy(New CommitFilter With {.IncludeReachableFrom = head, .ExcludeReachableFrom = recorded}).Count()`; equal shas → 0) and `src/CodeMem.Bridging/Status/MapStatusReader.vb` (`Read(config, map As MapDatabase) As MapStatusEnvelope`: `StoreAccess` → `CodeMapSolutionsRepository.ReadAll`; per active bound row `SolutionsRepository.ReadById` (Nothing → `map_missing_solution`) → `ExtractRunsRepository.ReadLatestCompleted` (Nothing, or `commit_sha` null → `no_git` with the reason) → `RepositoryFacts.Read` → verdict in the ruled order `map_missing_solution` → `no_git` → `dirty` → `diverged` → `behind` → `current`; `reason` one sentence; unbound and inactive lists; `bound` count); `src/CodeMem.Bridging/Reading/Envelopes/MapStatusEnvelope.vb` (data-model §7); implement `BridgeTools.MapStatus` in the T022 shape (the map opened and read inside one transaction; the repositories are asked after the map facts are read); register `map_status` (`ReadOnly = True`)
- [X] T034 [US3] Build; run `--filter "FullyQualifiedName~B04_|FullyQualifiedName~B01_"` → B04 (1)–(9) green, B01 (4) over seven tools; FIRE: swap `behind` before `dirty` in the verdict order → (3) red; revert → green; record in the header of `tests/CodeMem.Tests/Bridge/B04_MapStatusTests.vb`
- [X] T035 [US3] Add to `tests/CodeMem.Tests/Bridge/B08_LiveMapTests.vb` (6) `LiveMapStatusReportsMemOsBehind` — the MemOS entry: `verdict` `behind` or `dirty` (whichever `git status` says that minute — assert `behindBy` ≥ 1 and `head.sha` non-null and ≠ `run.commitSha`), the GameRoom and CodeMem entries present with non-null heads; `unbound` empty; the call timed with a `Stopwatch` around `BridgeHost.Call` and asserted under 3 s (SC-301 — G3: three `RetrieveStatus()` calls on live trees), the time printed. Run armed once; record the three verdicts, shas and the time in `specs/004-codemem-bridge/quickstart.md`
- [X] T036 [US3] Run the whole suite → green; record the run line in `specs/004-codemem-bridge/quickstart.md`

**Checkpoint**: US1–US3 independently green; the map can say whether it is current.

---

## Phase 6: User Story 4 — A green build refreshes the built solution, through the bridge, under two gates (Priority: P3)

**Goal**: The extract door with three origins, cardinality then configuration then the gates before any resolution
(INC2), resolution by directory containment that includes the root itself (COR1), the child process under a 540 s
budget inside the hook's 600 s (TIM1), the log beside the executable with one line per solution launched (INC3), the
`extract` tool, the `extract` and `hook` entries with positional targets parsed after options (COR2), the sample
config and the fragment.

**Independent Test**: B05 and B06 green (scripted launcher for the matrix, one real launch and one gate refusal
through the executable — CON3); the hook end to end in the quickstart's live step 6.

- [X] T038 [P] [US4] Create `tests/CodeMem.Tests/Bridge/B05_ExtractGateTests.vb` (a fixture map in a `TempMap`, a `RegistryFixture` bound to it, `BridgeHost` with a `ScriptedLauncher`, a fresh config file per fact via `BridgeHost.WriteConfig`; `extract.log` lives beside the executable — for in-process facts that is `AppContext.BaseDirectory` of the test host, shared by every fact, so each fact identifies its own lines by the unique key or path it used and counts those, never the file's length — INC3; B05 and B06 both carry `<Collection("ExtractLog")>` so xUnit runs the two classes in sequence and the shared log is appended by one class at a time — U2): (1) `BothGatesOffRefusesNamingEnabled` — every origin → `IsError`, text contains `extract.enabled is false`, `Launcher.Requests.Count` 0, map hash unchanged, one log line for this fact whose gate column is `extract.enabled`; (2) `EnabledOnlyRefusesTheHookAndRunsTheTool` — origin `GreenBuild` → `extract.onGreenBuild is false`, 0 launches; origin `Tool` with a scripted exit 0 and the summary line of the fixture's run → `launched` True, `exitCode` 0, `line` verbatim, `run.runId` and the ten counts equal to the map's row; (3) `OnGreenBuildOnlyRefusesNamingEnabled` — 0 launches, text names `extract.enabled`; (4) `BothOnRunsEveryOrigin` — three launches, three log lines; (5) `GateBeforeResolution` — both off and a `repoPath` under no root → the text names `extract.enabled`, not the path; (5b) `CardinalityBeforeConfiguration` (INC2) — none of the three arguments **and** a config file absent → `exactly one of solutionKey, repoPath or stale`, not `not configured`; (6) `PathUnderNoRootRefusesNamingThePath` (FR-347) — both on, `repoPath` `<temp>\nowhere-<guid>` → `No registered solution's repository root contains` + the path, 0 launches, hash unchanged, one log line; (7) `PathUnderTheRootResolvesToTheKey` — `repoPath` = a subdirectory of the fixture copy's root → `resolvedKey` `Sample`, the launch request carries `SolutionKey` `Sample`, `SolutionPath` = the map's `last_seen_path`, `MapPath` = the config's, `Budget` 540 s; (7b) `TheRootItselfResolves` (COR1) — `repoPath` exactly the registered root, with and without the trailing separator, in the other case → `resolvedKey` `Sample`; (8) `NestedRootsPickTheLongest` and `SameRootTwiceIsAmbiguous` — two registry rows bound to two map solutions with nested roots (a second solution extracted from a copy nested one level down) → the longer; two rows bound to solutions sharing one root → `more than one`; (9) `KeyRefusals` — unknown key → `No code_map_solutions row`; unbound → `no codemem_solution_id`; inactive → its state; bound to 99 → `holds no solution 99`; (10) `SecondCallWhileRunningIsRefused` — `Delay` 2 s on the launcher, two calls on two threads → one `launched`, one `still running`; (11) `StaleExtractsTheBehindAndDirtyOnes` — three bound solutions on a `GitFixture` (current, behind, dirty) → `considered` 3, `extractedCount` 2, the current one `extracted` False with its verdict, exactly two log lines for this fact (one per solution launched, no header — INC3); (12) `ChildExitCodesAreReportedVerbatim` — scripted exit 2 with a stderr line → `exitCode` 2, `line` = that line, `run` null, `IsError` True; exit 4 with a prefixed summary → `line` verbatim, `run` null; (12b) `ATimedOutChildIsReportedNotHidden` (TIM1) — `ScriptTimeout()` → `timedOut` True, `exitCode` null, `line` names 540 s, `run` null, `IsError` True, the log line's exit column `timeout`; (13) `OneRealLaunchOnTheFixtureOverStdio` (CON3) — `BridgeProcess.Serve(config)` with both gates on and the real launcher (the extractor beside the tests): `CallTool("extract", {solutionKey "Sample"})` → not an error, exit 0, summary line, `run` equal to `MapQueries.ReadRuns`' newest row, `elapsedMs` > 0; and `AGateRefusalOverStdio` — the same server with both gates off → `IsError` True naming `extract.enabled`; (14) `ALogLineThatCannotBeWrittenDoesNotStopTheRun` — hold `extract.log` open with `FileShare.None` for the duration of the call → `logged` False, `logError` non-null, `launched` True; (15) `TheExtractDescriptionNamesTheGates` — `extract.enabled`, `extract.onGreenBuild`, `Gated`, `never writes the map` present; (16) `TheGateIsReadPerCall` — one `BridgeHost`, both gates on, one call launched; rewrite its config file with `extract.enabled` false; the next call is refused naming `extract.enabled` with no further launch (the edge case "the configuration file changes between two calls"); (17) `AnEmptyRegistryRefusesEveryTarget` — a `RegistryFixture` with no rows: `extract(solutionKey "Sample")` → `No code_map_solutions row`; `extract(repoPath <the fixture root>)` → `No registered solution's repository root contains`; `extract(stale)` → `considered` empty, `extractedCount` 0; 0 launches. Run → the named Red: the assembly does not compile (`ScriptedLauncher`, `LaunchRequest`, `IExtractorLauncher`, `ExtractDoor`, `BridgeHost.Launcher` do not exist); record; after T037, run again → red on `NotImplementedException("US4")`; record
- [X] T039 [P] [US4] Create `tests/CodeMem.Tests/Bridge/B06_HookEntryTests.vb` (`<Collection("ExtractLog")>`, shared with B05 — U2) through `BridgeProcess.RunOnce("hook --config <path>", stdin)`: (1) `GreenPayloadResolvesAndAnswers` — config with both gates on, `mapPath`/`storePath` of a fixture map + registry whose bound solution's `repo_root` **contains the path named in the payload's command** (rewrite the payload's `.sln` path to the fixture copy's before piping); exit 0; stdout is exactly one JSON line whose `hookSpecificOutput.hookEventName` is `PostToolUse` and whose `additionalContext` starts with `codemem extract (green build,` and names `Sample` and `exit 0` (the launcher here is the real one — the fixture builds in ~3 s); one log line with origin `green_build`; (2) `FailurePayloadDoesNothing` — `posttoolusefailure-red.json` → exit 0, `additionalContext` contains `is not a Bash PostToolUse`, no log line, the map hash unchanged; (3) `CdPrefixAndProjectPathResolveTheProjectDirectory` — `posttooluse-cd-and-test.json` with its `cd` directory and `.vbproj` rewritten to the fixture copy's `Sample.Lib` → `resolvedKey` `Sample` (the `.vbproj`'s directory lies under the root); (3b) `OptionsBeforeThePathStillNameIt` (COR2) — command `dotnet build -c Debug "<fixture>\Sample.sln" --nologo` → `resolvedKey` `Sample`; (3c) `QuotedPathsAreOneToken` (COR2) — a copy under a directory with a space in its name, command `dotnet build "<dir with space>\Sample.sln"` → `Sample`; (3d) `ACrossRepositoryPathResolvesToItsOwnRoot` (COR2) — a second `FixtureCopy` registered as `Other` with its own root; the payload's `cwd` is the first copy's root and its command names the second copy's `.sln` → `resolvedKey` `Other`, never `Sample`; (3e) `TwoCandidatesAreAmbiguousNeverFallback` (COR2) — command `dotnet build A.sln B.sln` → `additionalContext` contains `more than one target` naming both, no launch, no fallback to `cwd`; (3f) `ANamedPathUnderNoRootIsRefusedNeverTheSessionDirectory` (Q6 as ruled — G1) — the payload's `cwd` is the first copy's registered root and its command is `dotnet build "<temp>\nowhere-<guid>\Nowhere.sln"` → `additionalContext` contains `No registered solution's repository root contains` and that path and never `Sample`; no new row in `MapQueries.ReadRuns`, the map hash unchanged; one log line whose resolution column is `PathNotRegistered`; (4) `InterruptedPayloadDoesNothing` — the green payload with `"interrupted":true` → `interrupted; nothing ran`; (5) `GateOffIsReportedNotThrown` — both gates off → exit 0, `additionalContext` contains `refused` and `extract.enabled`, one log line; (6) `NotJsonIsReported` — stdin `not json` → exit 0, `payload not JSON`; (7) `NotADotnetCommandDoesNothing` — the green payload with `tool_input.command` set to `ls -la` → exit 0, `not a dotnet build or test`, no log line, 0 launches; (8) `TheEntryIsFast` — facts (2), (4), (6) and (7) each return within 1 s of `RunOnce` elapsed (process start included; the entry's own cost was 17 ms in the spike), the times printed to the test output. Run → red with the assembly (B05's compile failure is the battery's named Red); after T037, run again → red on the `hook` branch's `NotImplementedException`; record both
- [X] T040 [US4] The directory-containment Red (COR1): add the fact `ContainsDirectoryIncludesTheRootItself` to `tests/CodeMem.Tests/Fixpack003/R01_ScopeRootTests.vb` (dated header line): for `SolutionScope.Resolve(root, root)`, `ContainsDirectory(root)` and `ContainsDirectory(root without its trailing separator)` are True, `ContainsDirectory(<root>\sub)` True, `ContainsDirectory(<parent of root>)` False, `ContainsDirectory(<root>2)` False (the same-prefix sibling), mixed case and forward slashes True; and `Contains(root)` keeps its file semantics unchanged. Run → red (no such method: a compile failure, the battery's named Red; still red after T037 until T041); record
- [X] T037 [US4] After T038–T040's Reds are recorded (CON2: the skeleton follows its facts — I6, analyze pass 2), land the seam and the skeletons so the three facts compile: `src/CodeMem.Bridging/Extract/ExtractOrigin.vb` (`Tool`, `GreenBuild`, `Manual`), `Extract/ExtractRequest.vb`, `Extract/ExtractResult.vb` (data-model §8, `timedOut` included), `Extract/StaleResult.vb`, `Extract/LaunchRequest.vb` (`ExtractorPath`, `SolutionPath`, `MapPath`, `SolutionKey`, `Budget As TimeSpan`), `Extract/LaunchResult.vb` (`ExitCode As Integer?`, `StandardOutput`, `StandardError`, `Elapsed`, `TimedOut`), `Extract/IExtractorLauncher.vb` (`Launch(request As LaunchRequest) As LaunchResult`), `Extract/ExtractDoor.vb` with `Run(request) As ExtractResult` and `RunStale(request) As StaleResult` throwing `NotImplementedException("US4")`; retype `BridgeTools`'s launcher parameter to `IExtractorLauncher`; create `tests/CodeMem.Tests/Support/ScriptedLauncher.vb` (`Implements IExtractorLauncher`; `Requests As List(Of LaunchRequest)`; `Script(exitCode, stdout, stderr)`; `ScriptTimeout()` (returns `TimedOut = True`, no exit code); optional `Delay As TimeSpan` for the running fact); give `BridgeHost` a `Launcher As ScriptedLauncher` and pass it to `BridgeTools`
- [X] T041 [US4] In `src/CodeMem.Extraction/Workspace/SolutionScope.vb` add `Public Function ContainsDirectory(directory As String) As Boolean` — the directory normalised as `Root` is (full path, separators, one trailing separator), then equality or `StartsWith(Root, OrdinalIgnoreCase)`; `Contains` untouched; dated header line (one additive method, no extractor behaviour change — the extractor never calls it). Run → the new fact green, every other R01 fact green; FIRE: make `ContainsDirectory` prefix-only (drop equality) → red on the root case; revert → green; record
- [X] T042 [US4] Create `src/CodeMem.Bridging/Extract/ExtractGates.vb` (`Check(config, origin) As BridgeRefusal` — `extract.enabled` False → `GateOff("extract.enabled")` for every origin; then `GreenBuild` and `extract.onGreenBuild` False → `GateOff("extract.onGreenBuild")`; else Nothing), `Extract/TargetResolver.vb` (`Resolve(request, config, store, map) As ResolvedTarget` — by key: `KeyNotRegistered` / `KeyInactive` / `KeyUnbound` / `MapMissingSolution`; by path: every active bound row with a map row whose `repo_root` is non-null, `SolutionScope.Resolve(root, root).ContainsDirectory(path)` (COR1: equality or prefix), longest root wins, equal lengths → `AmbiguousRoot` naming the keys, none → `PathNotRegistered` naming the path and the roots; the target carries `SolutionKey`, `SolutionPath` = `last_seen_path`, `MapPath`), `Extract/ExtractLog.vb` (`Append(line)`: `extract.log` **beside the executable** (`AppContext.BaseDirectory`), one tab-separated line per contracts/cli-config-hook.md §5, newlines removed; returns `(Logged, Error)` and never throws), `Extract/ProcessExtractorLauncher.vb` (`dotnet "<dll>" --solution "…" --db "…" --solution-key "…"` when the path ends in `.dll`, the `.exe` directly otherwise; `ExtractorNotFound` when the file is absent; redirected stdio, no window; **540 s** budget (TIM1) then kill and `TimedOut = True`; default path `Path.Combine(AppContext.BaseDirectory, "CodeMem.Extractor.dll")`), and fill `Extract/ExtractDoor.vb` in this order (INC2, CON4): argument cardinality (`TargetMissing`) → config → gate → `StoreAccess` → `Using map = MapAccess.OpenRead(config)` → resolve → `map.EndRead()` and dispose **before** launching (FR-334) → `SemaphoreSlim(1,1)` `Wait(0)` else `ExtractionRunning` → launch → `TimedOut` → `ChildTimedOut` result (`timedOut` True, `exitCode` null, `line` "extractor killed after 540 s", `IsError`); else summary line = last non-empty stdout line on exit 0/4, refusal line = first stderr line otherwise → reopen the map read-only and `ExtractRunsRepository.ReadById` of the `run_id=` the line names (only on exit 0) → release → `ExtractLog.Append` → result; `RunStale`: `MapStatusReader.Read`, the `behind`/`dirty`/`diverged` entries in sequence through `Run` with `solutionKey` (each writes its own log line; no header — INC3), the rest listed with their verdicts; every refusal on the wire is `IsError` True even though the result JSON is returned in the text
- [X] T043 [US4] Create `src/CodeMem.Bridging/Extract/HookRequest.vb` (`Shared Function Parse(json As String, cwd As String) As HookRequest`: the algorithm of contracts/cli-config-hook.md §1 — event/tool/interrupted checks; segments split on `&&`, `;`, `|`; the `dotnet build|test` segment; the effective directory from a preceding `cd`; then **positional targets after options** (COR2): tokenise the build segment with quoted tokens kept whole and quotes stripped; candidates are every token ending in `.sln` or `.vbproj` (case-insensitive) anywhere after the verb, plus the token immediately after the verb when it does not start with `-` and is not such a file (a positional directory); 0 candidates → the effective directory; 1 → its directory (a file) or itself (a directory), resolved against the effective directory, never checked for existence; ≥ 2 → `AmbiguousTarget` naming them, never a fallback; a `Refusal As String` for the named non-cases) and `src/CodeMem.Bridging/Extract/HookEntry.vb` (`Run(configPath, input As TextReader, output As TextWriter) As Integer`: read the input to end, parse, on a refusal print the one-line JSON and return 0; else `ExtractDoor.Run` with origin `GreenBuild`, print `{"hookSpecificOutput":{"hookEventName":"PostToolUse","additionalContext":"…"}}` with the line of §1 step 5, return 0 — never write anything else to the output); wire `src/CodeMem.Bridge/Program.vb`: `extract …` → `ExtractDoor` with origin `Manual` (or `GreenBuild` with `--on-green-build`), print the result JSON, exit 0 / 2 (refusal) / 1 (usage); `hook` → `HookEntry.Run(configPath, Console.In, Console.Out)`; implement `BridgeTools.Extract` (origin `Tool`; `stale` → `RunStale`) and register `extract` (`ReadOnly = False`, `Destructive = False`)
- [X] T044 [P] [US4] Create `src/CodeMem.Bridge/bridge.config.sample.json` (contracts/cli-config-hook.md §2 text with the two live paths, `extractorPath` null, both gates false) and `src/CodeMem.Bridge/hooks/settings.fragment.json` (§4 text, absolute path, `timeout` 600); mark both `<None>` with `CopyToOutputDirectory="Never"` in the project file (they ship in the tree, not beside the exe)
- [X] T045 [US4] Build; run `--filter "FullyQualifiedName~B05_|FullyQualifiedName~B06_|FullyQualifiedName~B01_"` → B05 (1)–(17) and B06 (1)–(8) green; B01 (4) skips `extract` and (5) lists eight tools; record GREEN lines in the headers of `tests/CodeMem.Tests/Bridge/B05_ExtractGateTests.vb` and `tests/CodeMem.Tests/Bridge/B06_HookEntryTests.vb`
- [X] T046 [US4] FIREs, each recorded: (a) in `src/CodeMem.Bridging/Extract/ExtractGates.vb` evaluate `extract.onGreenBuild` before `extract.enabled` → B05 (3) red; revert; (b) in `src/CodeMem.Bridging/Extract/ExtractDoor.vb` resolve before gating → B05 (5) red; revert; (c) in `src/CodeMem.Bridging/Extract/HookRequest.vb` accept `PostToolUseFailure` → B06 (2) red (a launch is attempted); revert; (d) in `src/CodeMem.Bridging/Extract/ExtractDoor.vb` skip the semaphore → B05 (10) red; revert; (e) in `HookRequest.Parse` take the first candidate when two are named → B06 (3e) red; revert; (f) in `ExtractDoor` treat `TimedOut` as exit 0 → B05 (12b) red; revert; (g) in `TargetResolver` use `Contains` instead of `ContainsDirectory` → B05 (7b) red; revert; (h) in `src/CodeMem.Bridging/Extract/HookEntry.vb`, on `PathNotRegistered` retry `ExtractDoor.Run` with the payload's `cwd` → B06 (3f) red (it resolves `Sample`) — G1; revert; (i) in `src/CodeMem.Bridging/Extract/ExtractDoor.vb` keep the read transaction open across the launch (dispose after the child exits) → B05 (13) red (the child cannot publish while the SHARED lock is held: exit ≠ 0) — G2, FR-334; revert → green
- [X] T047 [US4] Run the whole suite → green (the one real launch adds ~5 s); record the run line and the suite time in `specs/004-codemem-bridge/quickstart.md` (SC-311, under 5 min)

**Checkpoint**: the loop closes on the fixture: a green build's payload refreshes the built solution through
one door, under two gates, with one log line per solution launched.

---

## Phase 7: User Story 5 — One declaration, compiled into N projects (Priority: P4)

**Goal**: The presentation rule: raw rows preserved; `TwinFolder` folds only groups whose declaring projects are
all distinct (COR3); `symbol_detail`'s header names every twin; the map is untouched.

**Independent Test**: B07 green on a fixture copy with a linked file and a same-project pair; B08 (7) on the live
MemOS map when armed.

- [X] T048 [US5] Create `tests/CodeMem.Tests/Bridge/B07_TwinPresentationTests.vb` on a `FixtureCopy`: write `<copy>\Shared\Twin.vb` whose namespace depends on a compilation symbol so the two copies get distinct doc-comment ids under one root namespace — `#If TWIN_APP Then` / `Namespace Global.Sample.App.Shared` / `#Else` / `Namespace Global.Sample.Lib.Shared` / `#End If`, then `Public Class Twin` with `Public Function Name() As String` returning `"twin"`, `End Class`, `End Namespace`; `Replace` both project files on `</Project>` to add `<ItemGroup><Compile Include="..\Shared\Twin.vb" /></ItemGroup>`, and `Sample.App.vbproj` additionally `<DefineConstants>$(DefineConstants);TWIN_APP</DefineConstants>`; **and the same-project shape (COR3)**: write `Sample.Lib/OnOneLine.vb` with `Public Class OnOneLine` and two overloads declared on one physical line — `Public Sub M(a As Integer) : End Sub : Public Sub M(a As String) : End Sub` — (same kind, name, path, line, project; distinct doc ids); extract. Facts: (1) `TwinsPresentAsOneDeclaration` — `symbol_search(name "Twin", kind "class")`: `total` 1, one element with `compiledInto` of 2 naming `Sample.App` and `Sample.Lib` project ids and both symbol ids and both doc ids, `path` `Shared/Twin.vb` and one `line`; the `Name` method twins likewise fold (`kind "method"`, `total` 1); (2) `DetailOfEitherTwinNamesBoth` — `symbol_detail` of each id: `symbol.compiledInto` lists both with the requested id first; `parts` and `outbound` are the requested id's (the `part_of` target is its own project's namespace symbol), and every occurrence in `outbound` and `inbound` passes `OccurrenceAssertions.AssertIdentity`; (3) `TwinsAcrossSolutionsDoNotFold` — a second `FixtureCopy` of the same tree extracted into the same map with `--solution-key Other`: `symbol_search(solutionKey "Sample", name "Twin")` still `total` 1 with 2 entries, never 4; (4) `SameProjectRowsStayTwoDeclarations` (COR3) — `symbol_search(name "M", kind "method")` under `Sample`: `total` 2, two elements each with `compiledInto` of 1, both under `Sample.Lib`; and `symbol_search(name "MainForm", kind "class")` (a partial type: one symbol, two parts) → one element, `compiledInto` of 1; (5) `TheSearchDescriptionStatesTheFold` — `compiled into N projects` in the description. Run → (1)–(3) red (two rows for `Twin`, `compiledInto` of 1 each; `total` 2), (4) green (not Red: the mechanism it guards — no fold under one project — is the boundary of the rule that (1) makes Red-first; trusted through the T050 fire), (5) red; record
- [X] T049 [US5] Fill `src/CodeMem.Bridging/Reading/TwinFolder.vb` (the one door; in memory, COR3): group the raw rows by (`solution_id`, `kind`, `name`, `path`, `start_line`); a group folds into one declaration **only when its `project_symbol_id` values are all distinct and more than one**; any other group yields one declaration per row; twins ordered by symbol id; `total` = declarations; the 200 cap applied to declarations → `truncated`; in `src/CodeMem.Core/Repositories/CodeSymbolsRepository.vb` add `ReadTwins(db, id) As List(Of SymbolRecord)` (active rows of the same solution sharing the four fields, ordered by id — the folder decides whether they fold); `SymbolDetailReader` builds the header from `ReadTwins` through the same folder rule; update `BridgeToolDescriptions.SymbolSearch` if the fold sentence is not yet verbatim
- [X] T050 [US5] Build; run `--filter "FullyQualifiedName~B07_|FullyQualifiedName~B02_|FullyQualifiedName~B01_"` → B07 (1)–(5) green, B02 still green, B01 (4) green; FIRE: in `src/CodeMem.Bridging/Reading/TwinFolder.vb` drop the all-distinct check (fold every group of more than one row) → (4) red (`M`'s two overloads fold into one); revert → green; record in the header of `tests/CodeMem.Tests/Bridge/B07_TwinPresentationTests.vb`
- [X] T051 [US5] Add to `tests/CodeMem.Tests/Bridge/B08_LiveMapTests.vb` (7) `LiveTwinsFoldOnMemOs` — `symbol_search(solutionKey "MemOS", name "CodeMemMapFixture", kind "class")`: `total` 1, `compiledInto` ids 3556 and 4200; and the count of presented declarations across the whole MemOS map (`symbol_search` per kind, summing `total`) equals the active symbol count minus 222 (SC-309); every call timed with a `Stopwatch` and each asserted under 3 s (SC-301 — G3), the times printed. Run armed once; record in `specs/004-codemem-bridge/quickstart.md`

**Checkpoint**: all five stories independently green; the suite is the evidence.

---

## Phase 8: Polish, the document, the live steps

**Purpose**: What ships beside the code, the review pass, and the operator steps of the quickstart.

- [X] T052 [P] Create `src/CodeMem.Bridge/README.md` — the process document of contracts/cli-config-hook.md §6 in that order: what the bridge is (decisions 137077, 142362, 152658 by number; two projects, one process); install (build Release, copy the sample config beside the exe and set the paths, approve the project server, the user-scope line for other repositories, merge the fragment into `~/.claude/settings.json`, flip the gates — the Architect flips them); **when each tool is called** (`map_status` at session start and before relying on `references`, `orphans` or `type_usages`; `type_usages` before any delete or rename; `extract` by the hook, never by Claude Code directly; `extract --stale` by a human from the command line; `--on-green-build` never by a human); the gates; `extract.log` beside the executable; the refusal table from contracts/tools.md §6 with remedies; what the bridge never does; and the recorded non-choice: the `mcp_tool` hook type produced no observable effect in the 2026-09-15 spike, so the fragment uses the command hook and the `mcp_tool` type is not retried (STOP 1 ruling 3)
- [X] T053 [P] Review pass over every new and changed file against the v1.3.0 Review Gates (constitution): Option settings in both new project files; no SQL outside `Core/Repositories` and `Core/Schema` (`SqlLocationGateTests`, `BridgeSqlGateTests` green); header block and XML docs (`FileHeaderGateTests` green over `src/CodeMem.Bridge/**`, `src/CodeMem.Bridging/**` and `tests/CodeMem.Tests/Bridge/**`); the tripwire green; every new guard's FIRE line present (T007, T008, T011, T012, T013, T024, T030, T034, T041, T046, T050); one door per rule (list the doors and their callers in `plan.md` "Implementation record"); the two connection sites; the executable holds only `Program.vb`, `BridgeCommandLine.vb` and `Mcp/BridgeServer.vb` (CON1); no new abstraction beyond the launcher and read seams; `Version` 0.1.0 on both new projects, 0.2.0 on Extraction unchanged
- [X] T054 Run the whole suite unarmed → every test green, `AcceptanceRunner` and B08 Skipped; then armed (`CODEMEM_LIVE_MAP`, `CODEMEM_LIVE_STORE`) → B08 (1)–(7) green, the live map's hash equal before and after; record both run lines and the elapsed time in `specs/004-codemem-bridge/quickstart.md` "Build and test" (SC-311); `git -C C:\Users\rchau\source\repos\rchaudio-a11y\MemOS status --porcelain` empty before and after the armed run, recorded beside them (FR-349)
- [X] T055 Live step 1–2 (quickstart): `dotnet build CodeMem.sln -c Release`; copy `bridge.config.sample.json` beside `src/CodeMem.Bridge/bin/Release/net8.0/CodeMem.Bridge.exe` with both gates off; open a Claude Code session in `repos\CodeMem`, approve the `codemem` project server, `claude mcp list` shows it connected; hash `C:\_DB\codemem.sqlite`; call `solutions`, `symbol_search(projectId 132040, name "btnDeal_Click")`, `symbol_detail(1200)`, `references(1200)`, `orphans(solutionKey "GameRoom")`; hash again; run the same five through the MemOS Shell and fill the side-by-side table in `specs/004-codemem-bridge/quickstart.md` (SC-302, with any audit-only field removed; a differing orphan count is a Red to diagnose, and only the Architect accepts a changed number)
- [X] T056 Live step 3–4: `type_usages(3023, solutionKey "MemOS")` and `references(3023)`; `symbol_search(solutionKey "MemOS", name "CodeMemMapFixture", kind "class")`; `map_status()`; fill the "Record" rows of `specs/004-codemem-bridge/quickstart.md` (SC-303, SC-304, SC-309)
- [X] T057 Live step 5, part one: with both gates off, `extract(solutionKey "MemOS")` → refused naming `extract.enabled`, the live map's hash unchanged, one line in `extract.log` beside the Release executable; record in `specs/004-codemem-bridge/quickstart.md`
- [X] T058 Live step 5, part two — **on a copy**: `Copy-Item C:\_DB\codemem.sqlite <scratch>\codemem.004-copy.sqlite`; point `mapPath` at the copy; flip `extract.enabled` on; `extract(solutionKey "MemOS")` → exit 0, run 7 at HEAD `cbeb661` (or the HEAD of that minute), the summary line and ten counts recorded, elapsed under 540 s; `map_status()` → MemOS `current`; hash `C:\_DB\memos.sqlite` before and after: unchanged; restore `mapPath` to the live file and `extract.enabled` to off; record verbatim in `specs/004-codemem-bridge/quickstart.md`
- [X] T059 Live step 6 — the hook (the Operator's hand, not this feature's code): merge `src/CodeMem.Bridge/hooks/settings.fragment.json` into `~/.claude/settings.json`; with `mapPath` at the copy and both gates on, in a session in `repos\CodeMem` run `dotnet build CodeMem.sln`: the session shows the bridge's line (exit 0, the CodeMem summary line); with `extract.onGreenBuild` off, the line names that gate; from a session in an unregistered directory, a `dotnet build` there names the path; the tool call never fails; record (SC-310); restore the config
- [X] T060 Live step 7 — **only at the Architect's request, after a backup** `C:\_DB\codemem.pre-004.<date>.sqlite`: `mapPath` at the live file, `extract(solutionKey "MemOS")` or the hook on a green MemOS build → run 7; `map_status()` → MemOS `current`; `memos.sqlite` hash unchanged; `git -C …\rchaudio-a11y\MemOS status --porcelain` empty before and after; record in `specs/004-codemem-bridge/quickstart.md`; leave both gates as the Architect sets them
- [X] T061 Write the "Implementation record (<date>)" section of `specs/004-codemem-bridge/plan.md` (environment, the order followed, every Red observed and every FIRE, the doors and their callers, deviations from the design documents with reasons, the live figures) and the "Known limits" carried (G8 among them); the analyze pass 2 LOW items carried to implementation (I7–I12, D1, G5, U4, U5, A1 — spec Clarifications, review rulings pass 2), each fixed when its file was touched, any still open listed by tag; update `specs/004-codemem-bridge/spec.md`'s Status line; the commits are the Architect's

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: T001 first; T003–T009 in parallel after T002 (T003–T006 and T009 need nothing from T002 but the tree; T007–T008 read the new project files).
- **Foundational (Phase 2)**: after Phase 1; **T010 (the B01 Red) before every slice** (CON2); T011 opens with the skeleton the B01 facts reference, so the assembly compiles for every run that follows (I1); T011 → T012 → T013 in order (each records a Red or a FIRE the next depends on); T014 after T013; T015–T016 close the phase. **Blocks every story.**
- **US1 (Phase 3)**: after Phase 2; **T017 (the B02 Red) before T018–T022** (CON2: the shared Core reads and the Bridging foundation land behind it). The MVP.
- **US2 (Phase 4)**, **US3 (Phase 5)**: after US1 (both reuse `MapAccess.OpenRead`, `ScopeResolver` and the refusal helper of T021); independent of each other. Sequenced here because the suite grows in one order.
- **US4 (Phase 6)**: after **US3** (`RunStale` calls `MapStatusReader`); T038, T039 and T040 first and in parallel (three test files; their compile failure is the battery's Red — CON2, I6); then T037; T040 → T041 (the extractor's additive method, Red then Green); T042 → T043; T044 in parallel with T042–T043; T045–T047 close.
- **US5 (Phase 7)**: after US1 (it fills `TwinFolder` and touches the two readers).
- **Polish (Phase 8)**: T052–T053 in parallel after Phase 7; T054 after both; T055–T060 in order, each recording before the next; T060 only at the Architect's request; T061 last.

### Within Each User Story

- The Red battery, run and recorded → skeletons only for the facts just written → Core read methods → readers/envelopes → the tool registered → Green recorded → FIREs recorded → the live fact added to B08 → the suite.

### Parallel Opportunities

- Phase 1: T003, T004, T005, T006, T007, T008, T009 (seven files, no shared edits).
- Phase 6: T038 ∥ T039 ∥ T040 (three test files, before T037); T044 ∥ T042–T043.
- Phase 8: T052 ∥ T053.
- Different stories can be built by different hands after US1, with US4 waiting for US3 and US5 for US1.

---

## Parallel Example: Phase 1 after T002

```text
Task: "Create tests/CodeMem.Tests/Fixtures/Hooks/*.json from the spike payloads"        (T003)
Task: "Create tests/CodeMem.Tests/Support/RegistryFixture.vb"                            (T004)
Task: "Create tests/CodeMem.Tests/Support/GitFixture.vb"                                 (T005)
Task: "Create tests/CodeMem.Tests/Support/BridgeProcess.vb"                              (T006)
Task: "Create tests/CodeMem.Tests/Guards/MemOsReferenceGateTests.vb and fire it"         (T007)
Task: "Create tests/CodeMem.Tests/Guards/BridgeSqlGateTests.vb and record its Red"       (T008)
Task: "Create .mcp.json"                                                                 (T009)
```

## Parallel Example: User Story 4 — the Red battery, before T037

```text
Task: "Create tests/CodeMem.Tests/Bridge/B05_ExtractGateTests.vb"                        (T038)
Task: "Create tests/CodeMem.Tests/Bridge/B06_HookEntryTests.vb"                          (T039)
Task: "Add the ContainsDirectory fact to Fixpack003/R01_ScopeRootTests.vb"               (T040)
Task: "Create bridge.config.sample.json and hooks/settings.fragment.json"                (T044, alongside T042–T043)
```

---

## Implementation Strategy

### MVP First (User Story 1 only)

1. Phase 1 and Phase 2 (the B01 Red, the doors, the registry, the server skeleton).
2. Phase 3: the B02 Red, then the shared reads, the foundation and the five ported readers.
3. **STOP and VALIDATE**: B01, B02 green; B08 (1)–(4) armed once; a Claude Code session in `repos\CodeMem`
   lists five tools and answers with the map untouched. That is the ruling of 142362 delivered.

### Incremental Delivery

1. US2 (`type_usages`) — the finding-152647 remedy; one tool, one Core query.
2. US3 (`map_status`) — the finding-152646 detection half.
3. US4 (`extract` and the hook) — the remedy half, gated off until the Architect flips it.
4. US5 (twins) — the finding-152648 presentation rule.
5. Phase 8 — the document, the live steps, the record.

Each story leaves the previous ones green; the map is never written by anything but the extractor.

---

## Notes

- [P] tasks = different files, no dependencies.
- Every Red and every FIRE is recorded in the test file header the moment it is observed; the plan's
  "Implementation record" (T061) collects them.
- No file under `rchaudio-a11y\MemOS` is read for code or changed; `git status` there is part of T054's and
  T060's records.
- The live files under `C:\_DB\` are written by exactly one task, T060, and only at the Architect's request.
- Stop at any checkpoint to validate the story on its own.
