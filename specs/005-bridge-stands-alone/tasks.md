---

description: "Task list for CodeMem 005 — the bridge stands alone: one database, .slnx as an equal input, warnings as warnings"
---

# Tasks: CodeMem 005 — The Bridge Stands Alone

**Input**: Design documents from `/specs/005-bridge-stands-alone/`

**Prerequisites**: plan.md (research R60–R73 proven by the scratch harness; §STOP 1's ten decisions **ruled 2026-09-17, all as
proposed** — each task names the decision it rests on), spec.md (clarified 2026-09-16, five rulings), research.md, data-model.md, contracts/tools.md,
contracts/cli-config-hook.md, contracts/extractor.md, quickstart.md — all present. Constitution **v1.3.0** governs until T003 lands
**v1.4.0**. 004's code and its 153 tests are the starting point (`main` at `90d62ca`; 145 passed / 8 skipped / 2 m 33 s). **Branch**:
`005-bridge-stands-alone` from `90d62ca`; the 005 artefacts are committed there before the first Red (the Architect's instruction
pattern of 004).

**Tests**: REQUIRED. FR-433–FR-437 and Article II make every behaviour a Red-first fact against real SQLite, the real compiled fixture
(in both forms), real NuGet restore results, a real repository and the real executables: write → run → confirm Red *for the stated
reason* → record it in the test file header → implement → confirm Green → record. Every guard carries a `' FIRE:` line. New tests:
`tests/CodeMem.Tests/Bridge/B09`–`B11`, `Extraction/X01`–`X02`, `Fixpack/S03`, `Guards/BridgeStandaloneGateTests`; amended in place with
a dated header line after their named Red: B01, B02, B04, B05, B06, B08, F06, S02, US1, `SqlLocationGateTests`, `BridgeSqlGateTests`.
**Order rule (CON2)**: no implementation slice lands before its behaviour test exists and is observed Red; a skeleton may land only
after the fact it will satisfy is written. **Production route (CON3)**: every new refusal and shape is driven once through
`CodeMem.Bridge.exe` over stdio or its `extract`/`hook` entries, and the extractor's through `CodeMem.Extractor.dll` as a child; `BridgeHost`
serves the fast in-process facts and is never the production-route evidence. **No second stop**: the work stops only for a Red not
named in plan §Test design.

**Organization**: Phase 1 setup (the branch, the standalone gate's Red, the amendment, the fixtures), Phase 2 foundational (schema
version 3 and the bridge's pin — every story's fixture map is version 3), Phases 3–7 the five stories in priority order (US1 scope by
key · US2 resolution from the map and the not-in-map answer, closing with the archive · US3 adding a solution and `not_in_map` ·
US4 `.slnx` · US5 warnings), Phase 8 polish, the document and the live steps. **The MVP is Phases 1–4**: US1 and US2 are both P1 and
the one-database gate (US1 scenarios 4–5) can only go green once US2 has taken the store out of the extract path and T024 has archived
it.

**Analyze (2026-09-17)**: nineteen findings, all accepted by the Architect and applied in this file, spec.md, plan.md, research.md and
quickstart.md (A1 measurement only; I4 aligned on SC-406's 5 s); the finding IDs (I1, C1–C3, G1–G5, U1–U5, A1–A2, I2–I5) are cited
where a task changed. **STOP 1 ruled 2026-09-17: all ten as proposed.**

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: US1–US5 from spec.md
- Every path is repository-relative

## Standing rules for every task that creates or edits a `.vb` file

- Header block first (`' File:`, `' Project:`, `' Description:`, `' Author: RCH Automation LLC`, `' Created: <date>`); XML docs on every public declaration; one type per file; Option Strict/Explicit On, Infer Off (project-wide).
- **No SQL literal anywhere in `src/CodeMem.Bridge/` or `src/CodeMem.Bridging/`**; every query is a named method under `src/CodeMem.Core/Repositories/` or `Schema/`. After T024, `New SqliteConnection` appears in `MapDatabase.vb` only (v1.4.0). **No path naming the store in either bridge project**: zero occurrences of the case-sensitive text `memos` (FR-401's gate; analyze I1) — prose may say `MemOS`, as the `ProjectIdRemoved` text does; the archive README is where the file name lives.
- The extractor reads no environment variable but its two test seams; **no `NoWarn` anywhere** (R62). There is no `--key`; `--solution-key` is the one spelling (Q3 as ruled).
- Red/Green/FIRE lines go in the test file header; every FIRE injection is reverted before the task closes.
- Retired code is **moved** (`git mv`) to `_Archive/004-store/` with its header intact, never deleted (Article XIV, R69); a retired test fact is recorded as text in that folder's README.
- The committed fixtures under `tests/CodeMem.Tests/Fixtures/` are never edited by a test; every mutation runs on a `FixtureCopy`. Live files under `C:\_DB\` are read-only in every task but T044–T045, which say otherwise.
- Nothing in `rchaudio-a11y\MemOS` is read for code; nothing there is changed; `memos.sqlite` is never opened by anything this feature builds (FR-436).

---

## Phase 1: Setup — the branch, the standalone gate's Red, v1.4.0, the fixtures

**Purpose**: The feature branch carries the artefacts; the gate that defines "standalone" exists and is Red; the constitution says what
the code will do; the `.slnx` twin and the two warning fixtures are in the tree before any fact needs them.

- [X] T001 Create branch `005-bridge-stands-alone` from `main` at `90d62ca` (`git switch -c 005-bridge-stands-alone 90d62ca`), commit `specs/005-bridge-stands-alone/` (spec, plan, research, data-model, contracts/, quickstart, checklists/) on it as the first commit; confirm `git status` clean, `dotnet build CodeMem.sln --nologo -v q` 0 errors / 0 warnings, `dotnet test --nologo -v q` 145 passed / 8 skipped. Record the confirmation line under "Build and test" in `specs/005-bridge-stands-alone/quickstart.md`
- [X] T002 Create `tests/CodeMem.Tests/Guards/BridgeStandaloneGateTests.vb` (FR-401, FR-402, FR-431; STOP 1 decisions 1 and 7) with four facts using `SqlLocationGateTests.SourceFiles`: (1) `TheBridgeProjectsOpenOnlyTheMap` — over every `.vb` under `src/CodeMem.Bridge/` and `src/CodeMem.Bridging/` (assert the file count > 0 first: "the scan is vacuous"), 0 occurrences of `SqliteConnection` and 0 occurrences of the case-sensitive text `memos` (Ordinal, whole file text: the file name `memos.sqlite` and any lowercase path segment; `MemOS` in prose — the `ProjectIdRemoved` text — is not a path and passes; analyze I1), naming file and line on failure; (2) `TheArchivedNamesAreAbsent` — the identifiers `StoreDatabase`, `CodeMapSolutionsRepository`, `RegistryTableMissingException`, `RegistryRecord`, `StoreAccess`, `UnboundEntryEnvelope`, `InactiveEntryEnvelope`, `RegistryFixture` appear in no `.vb` file under `src/` or `tests/` (this test file excluded by name); (3) `ArticleNineReadsAsVersionOneTwoOne` — a `Const` holding the v1.2.1 body of Article IX verbatim (the current article's text minus the paragraph that begins "One exception is ruled"; take it from `git show 6dc046a^:.specify/memory/constitution.md`), compared to the text between `### IX.` and `### X.` of `.specify/memory/constitution.md` with line endings normalised; (4) `TheConstitutionIsVersionOneFour` — the `**Version**:` line begins `**Version**: 1.4.0`. Run → all four **red for their stated reasons** ((1): `BridgeConfig.vb`'s XML doc reads `memos.sqlite`; (2): the names are everywhere; (3), (4): v1.3.0); record the four Red lines. (1) and (2) stay red until T024; (3) and (4) go green at T003
- [X] T003 Amend `.specify/memory/constitution.md` to **v1.4.0** exactly as research R70 states (STOP 1 decision 1): remove the paragraph of Article IX beginning "One exception is ruled" and nothing else in the article; replace the Review Gates' connection-site line with "New SqliteConnection appears in exactly one production file, MapDatabase.vb (the map: read-write for the extractor, read-only for the bridge), asserted by a test that names it and fires when a second site appears (v1.4.0; the v1.3.0 second site is archived)."; append R70's Amendment Record entry verbatim; set the last line to `**Version**: 1.4.0 | **Ratified**: 2026-09-10 | **Last Amended**: 2026-09-16`. Run `BridgeStandaloneGateTests` → (3) and (4) green; FIRE (3): change one word of the article → red naming the first differing line; revert → green; record. Commit the amendment on its own ("docs(constitution): v1.4.0 — Article IX returns to v1.2.1; the connection gate at one file")
- [X] T004 [P] Create `tests/CodeMem.Tests/Fixtures/Sample/Sample.slnx` with exactly the two-line body of research R61 (`<Solution>` root; `<Project Path="Sample.Lib/Sample.Lib.vbproj" />`; `<Project Path="Sample.App/Sample.App.vbproj" />`); add `Public ReadOnly Property SolutionXPath As String` (= `Path.Combine(FixtureDirectory, "Sample.slnx")`) to `tests/CodeMem.Tests/Support/FixtureSolution.vb` with a dated header line; one restore covers both (restore is per project)
- [X] T005 [P] Create the warning fixtures under `tests/CodeMem.Tests/Fixtures/Warnings/` (R71; STOP 1 decision 6): `NetFxOnly/NetFxOnly.vbproj` (net8.0 class library, one class `Marker`, `PackageId` `CodeMem.NetFxOnly`, `Version` 1.0.0, `IncludeBuildOutput` false, `PackageOutputPath` `..\packages`, `<None Include="bin\Debug\net8.0\NetFxOnly.dll" Pack="true" PackagePath="lib\net461\" />`) and pack it once by hand — `dotnet build` then `dotnet pack --no-build` — so `packages/CodeMem.NetFxOnly.1.0.0.nupkg` (≈ 3.6 KB) is committed; `NuGet.config` (`<clear />`, one source `packages`); `Nu1701/Nu1701.vbproj` (net8.0, one class `Greeter`, `PackageReference` `CodeMem.NetFxOnly` 1.0.0); `Nu1101/Nu1101.vbproj` (net8.0, one class, `PackageReference` `CodeMem.NoSuchPackage` 1.0.0). The test project's `<Compile Remove="Fixtures/**" />` already keeps the fixture `.vb` files out of the build, and the repository's `.gitignore` already ignores `bin/` and `obj/` (analyze U5). Create `tests/CodeMem.Tests/Support/WarningsFixture.vb` (`IAsyncLifetime`; `Nu1701ProjectPath`, `Nu1101ProjectPath`; `InitializeAsync` runs `dotnet restore` on each project's directory offline — the Nu1101 restore exits non-zero by design and still writes its assets file, as observed) and `tests/CodeMem.Tests/Support/WarningsCollection.vb` (`<CollectionDefinition("Warnings")>`). Verify by hand: `dotnet restore Nu1701` prints `warning NU1701`; `dotnet restore Nu1101` prints `error NU1101` naming the local source only
- [X] T006 [P] Add `Public Function ReadWarnings(db As String, runId As Long) As List(Of (Code As String, ProjectPath As String, Message As String))` to `tests/CodeMem.Tests/Support/MapQueries.vb` (`SELECT code, project_path, message FROM extract_run_warnings WHERE run_id = @run_id ORDER BY id`; the allowed test SQL file), with a dated header line

**Checkpoint**: the branch carries the artefacts and v1.4.0; the standalone gate exists with two Reds outstanding; the fixtures restore
offline; no product code has changed.

---

## Phase 2: Foundational — schema version 3 and the bridge's pin

**Purpose**: The map moves to version 3 with its warning table, in place, behind the Reds of S03 and of 002's version facts; the bridge
reads version 3. Every story after this runs on version-3 fixture maps.

**⚠️ CRITICAL**: No user story work until T011 records Green.

- [X] T007 Create `tests/CodeMem.Tests/Fixpack/S03_SchemaVersion3Tests.vb` (FR-429, FR-430; R63; STOP 1 decision 4; `<Collection("Fixture")>`) with five facts: (1) `AFreshMapIsVersionThreeWithTheWarningTable` — extract the fixture into a `TempMap`; `MapQueries.ReadSchemaVersion` = 3; `sqlite_master` holds `extract_run_warnings` and `ix_extract_run_warnings_run_id`; (2) `AVersionOneMapUpgradesToThree` — the committed version-1 map copy S02 uses → run → 3, every prior row kept plus the run's own; (3) `AVersionTwoMapUpgradesToThree` — a version-2 map (extract with today's build into a `TempMap` **before** T009 and commit it as `tests/CodeMem.Tests/Fixtures/Maps/version2.sqlite`, the version-1 copy's sibling, with a `<None Update="Fixtures/Maps/version2.sqlite" CopyToOutputDirectory="PreserveNewest" />` line beside `sample-v1.sqlite`'s in `tests/CodeMem.Tests/CodeMem.Tests.vbproj` — analyze U4) → run → 3, rows kept; (4) `AbortDuringUpgradeOnAVersionTwoMapLeavesItAtTwo` — `CODEMEM_TEST_ABORT_AT=DuringUpgrade:<nonce>` + `CODEMEM_TEST_NONCE` on a version-2 copy → the child exits non-zero naming the phase, `ReadSchemaVersion` = 2, the table **present** (the statements ran; the version did not move), the next run → 3; (5) `FreshAndUpgradedSchemasAreByteIdentical` — the `sqlite_master` `sql` texts of a fresh map, a map upgraded from 1 and a map upgraded from 2, ordered by name, equal. Run → (1) red (reads 2), (3) red (version 2 is `Current` today, so the run leaves it at 2), (4) red, (5) red (no table); record
- [X] T008 Record the **named Reds** of 002's version facts before touching Core: `tests/CodeMem.Tests/Fixpack/F06_FreshMapTests.vb` (two `Assert.Equal(2, …)`), `tests/CodeMem.Tests/Fixpack/S02_SchemaUpgradeTests.vb` ((1) and (4) assert 2; (3) hand-bumps to 3 to provoke the "newer" refusal), `tests/CodeMem.Tests/Invariants/US1_FirstRunTests.vb` (asserts 2) — they are green now and go red at T009; write the dated header lines that say so ("RED: <date> (005, T009): 2 → 3 …") and leave the asserts untouched until T009's Green
- [X] T009 Land version 3 in Core and the extractor (R63; contracts/extractor.md §5): `src/CodeMem.Core/Schema/SchemaVersion.vb` `Current = 3` (dated header line); `src/CodeMem.Core/Schema/SchemaState.vb` + `Version2` ("map_identity.schema_version = 2: migrate to 3 in place"); `src/CodeMem.Core/Schema/SchemaRepository.vb` + `Private Const MigrationToVersion3` (the DDL of contracts/extractor.md §5 verbatim, comments included, without the final UPDATE) and `Public Sub UpgradeToVersion3(db As MapDatabase)`; the version-1 and version-2 constants untouched; `src/CodeMem.Core/Repositories/MapDatabase.vb` `InspectSchema`: `version = 2` → `Version2` (before the `Current` test); (the warning record, the repository and the write land at T034–T036 behind X02's Red — analyze C1; nothing writes the table yet); `src/CodeMem.Extraction/Run/ExtractionRun.vb` step 4 — `Fresh`: `CreateVersion1`, identity, `AbortIf(DuringInitialize)`, `UpgradeToVersion2`, `UpgradeToVersion3`, `AbortIf(DuringUpgrade)`, `SetSchemaVersion(SchemaVersion.Current)`; `Version1`: `UpgradeToVersion2`, `UpgradeToVersion3`, `AbortIf(DuringUpgrade)`, `Set`; `Version2`: `UpgradeToVersion3`, `AbortIf(DuringUpgrade)`, `Set`; `src/CodeMem.Extraction/Run/RunPhase.vb` `DuringUpgrade`'s XML doc: "after the last migration statement of whichever path ran, before schema_version is written". Build; run S03 → (1)–(5) green; F06, S02 (1)(4), US1 → **red as named at T008** → amend each assert to 3 and S02 (3)'s hand-bump to 4, dated header lines; run `Fixpack`, `Invariants`, `Guards` → green. FIRE S03 (4): move `AbortIf(DuringUpgrade)` after `SetSchemaVersion` → the map reads 3 after the abort → red; revert → green; record
- [X] T010 Move the bridge's pin: `src/CodeMem.Bridging/Reading/BridgeSchemaPin.vb` `Required = 3` (dated header line). In `tests/CodeMem.Tests/Bridge/B02_PortedReaderTests.vb` record the Reds the pin causes — the version-1 fixture still refuses `VersionBelow` (green), the "hand-bumped 3" `VersionAbove` fact goes red (3 is now current) — then amend: the bump to 4 and one added fact `AVersionTwoMapIsRefusedBelowWithTheExtractorRemedy` over `Fixtures/Maps/version2.sqlite` (the text contains "Run the CodeMem extractor once: it upgrades the map in place"); dated header lines. Every other bridge test regenerates its fixture map by running the extractor and is at version 3 without change. Run `Bridge` → green
- [X] T011 `dotnet test --nologo -v q` → every test green (the amended F06/S02/US1/B02 included; `BridgeStandaloneGateTests` (1) and (2) still red, as named); record the run line and count in `specs/005-bridge-stands-alone/quickstart.md` "Build and test"

**Checkpoint**: the map is version 3 on every path; warnings have a table, still empty (the repository waits for X02's Red — analyze C1); the bridge reads version 3;
nothing about scope, resolution or the store has changed.

---

## Phase 3: User Story 1 — The bridge answers from the map and nothing else (Priority: P1) 🎯 MVP (with Phase 4)

**Goal**: Every read tool and `map_status` answer by `solutionKey` from the map alone; `projectId` and `storePath` are refused by name;
the descriptions no longer speak of a registry.

**Independent Test**: B09 (1)–(5) on a fixture map with no store file anywhere and no `storePath`; every tool with `projectId`; a
config with `storePath`; the descriptions. (B09 (4)–(5) of the spec — the gate facts — go green at T024.)

- [X] T012 [US1] Create `tests/CodeMem.Tests/Bridge/B09_StandaloneTests.vb` (FR-403, FR-405–FR-408, FR-418, FR-433; STOP 1 decision 5; `<Collection("Fixture")>`) with five facts: (1) `EveryToolAnswersWithNoStoreAnywhere` (CON3) — a fixture map (`FixtureMapScenario` without a store), a config written by `BridgeHost.WriteConfig(mapPath, extractorPath, enabled, onGreenBuild)` (**no `storePath` parameter**), `BridgeProcess.Serve`; call `solutions`, `symbol_search(solutionKey "Sample", name "Widget")`, `symbol_detail`, `references`, `orphans`, `type_usages`, `map_status` → none is an error, > 0 calls made; (2) `ProjectIdIsRefusedByNameOnEveryTool` — through the executable, each of the eight with `projectId: 132040` (beside its normal arguments) → `IsError`, text contains "projectId is not an argument"; and with a config path that does not exist → the same refusal (arguments before configuration); (3) `StorePathInTheConfigIsRefusedByName` — a config carrying `"storePath": "x"` → `solutions` and `extract(solutionKey "Sample")` both refuse with "no longer a key" naming `storePath`; (4) `TheDescriptionsSpeakOfNoRegistry` — `ListToolDescriptions()` equals the constants; none contains `projectId`, `registry`, `code_map_solutions` or `MemOS project`; every 004 FR-312 phrase still present (reuse B02's phrase table); (5) `TheEnvelopesCarryNoRegistryFields` — `map_status`'s JSON has no `storePath`, `bound`, `unbound`, `inactive`, and its entries no `projectId`/`codememSolutionId` but a `solutionId`; `symbol_search`'s `scope` has `by`, `solutionKey`, `solutions` and nothing else. Header "RED: <date> — the signatures the facts name do not exist (compile failure recorded as the Red now that the facts are written; the slices land in T013–T017)"
- [X] T013 [US1] Refusal vocabulary, first half (FR-409; contracts/tools.md §6): in `src/CodeMem.Bridging/Refusals/BridgeRefusalKind.vb` add `ProjectIdRemoved`, `ConfigKeyRetired`, `PathNotInMap`, `AmbiguousSolutionFile`; remove `ScopeConflict` and `RegistryAbsent` (the five extract-side kinds go at T021); in `src/CodeMem.Bridging/Refusals/BridgeRefusal.vb` add the four texts of §6 (`PathNotInMap` with its two shapes keyed on the fact `file` being present, `AmbiguousSolutionFile`, `ProjectIdRemoved`, `ConfigKeyRetired`), revise `ScopeMissing`, `SolutionKeyUnknown` (the add remedy appended), `TargetMissing` and `AmbiguousRoot` to §6's texts, and drop `Unopenable`'s store branch; dated header lines. Then `src/CodeMem.Bridging/Configuration/BridgeConfig.vb` drop `StorePath`; `src/CodeMem.Bridging/Configuration/BridgeConfigFile.vb` — after the JSON parse and before `RequiredString(root, "mapPath")`, `If root.TryGetProperty("storePath", …) Then Throw` `ConfigKeyRetired` with `key = storePath` and the file; `BridgeHost.WriteConfig` loses its `storePath` parameter (every caller updated in the same task)
- [X] T014 [US1] Scope by key (FR-405, FR-406; R68; STOP 1 decision 5): `src/CodeMem.Bridging/Reading/ScopeResolver.vb` — `ValidateArguments(rawArguments As IReadOnlyDictionary(Of String, JsonElement), solutionKey As String)`: any key equal to `projectId` (ordinal, then OrdinalIgnoreCase) → `ProjectIdRemoved`; a missing key → `ScopeMissing`; `Resolve(solutionKey, config, map)` — `ReadByKey` or `SolutionKeyUnknown`; the registry branch removed; `src/CodeMem.Bridging/Reading/Envelopes/ScopeEnvelope.vb` — `By`, `SolutionKey`, `Solutions` only; `src/CodeMem.Bridging/Mcp/BridgeTools.vb` — every tool method gains `context As RequestContext(Of CallToolRequestParams)` as its first parameter and loses `projectId`; `RunRead` loses `needsRegistry` and the store stage; each method passes `context.Params.Arguments` to `ValidateArguments` (also `Solutions` and `MapStatus`, which take no other argument); `src/CodeMem.Bridge/Mcp/BridgeServer.vb` — the delegate types gain the context parameter (`Func(Of RequestContext(Of CallToolRequestParams), CallToolResult)` for `solutions`/`map_status`, and so on); `tests/CodeMem.Tests/Support/BridgeHost.vb` — `Call(name, args)` builds a `RequestContext(Of CallToolRequestParams)` over the dictionary. Build; run B09 (2) → green if the SDK injects the context (record "confirmed"); if the SDK does not bind it in a VB delegate, land R68's fallback (one wrapping delegate per tool over `CallToolRequestParams` with a hand-written input schema) and record which landed in plan.md §Design
- [X] T015 [US1] `map_status` from map rows (FR-418; Q5, Q6): `src/CodeMem.Bridging/Status/MapStatusReader.vb` — `Read(config, map)`: one entry per `SolutionsRepository.ReadAll(map)` row; the root is `SolutionScope.Resolve(solution.RepoRoot, Path.GetDirectoryName(solution.LastSeenPath)).Root` (the one door; a NULL `repo_root` no longer yields `no_git` for "no repository root recorded" — the base directory is asked instead, and `no_git` remains for a run with no commit or an unreadable root); verdict order `no_git` → `dirty` → `diverged` → `behind` → `current`; the `map_missing_solution` branch removed; `src/CodeMem.Bridging/Reading/Envelopes/MapStatusEnvelope.vb` — drop `StorePath`, `Bound`, `Unbound`, `Inactive`; add `NotInMap As List(Of NotInMapEntryEnvelope)` (empty until T028) and `NotInMapError As String`; `src/CodeMem.Bridging/Reading/Envelopes/NotInMapEntryEnvelope.vb` (new: `Path`, `Verdict`, `ObservedUtc`, `Origin`, `SolutionFiles`, `SuggestedKey`, `Command`, `Note`); `src/CodeMem.Bridging/Reading/Envelopes/MapStatusEntryEnvelope.vb` — drop `ProjectId`, `CodememSolutionId`; add `SolutionId`; `src/CodeMem.Bridging/Extract/ExtractDoor.vb` `RunStale` — no registry read; `MapStatusReader.Read(config, map)`. Amend `tests/CodeMem.Tests/Bridge/B04_MapStatusTests.vb` (Reds recorded first): entries from map rows; the `map_missing_solution` fact and the unbound/inactive listing fact retired (cut into `_Archive/004-store/tests/B04_RetiredFacts.vb` at T024 — analyze C2; a dated header line here); add `ASolutionWithoutARepositoryRootResolvesAtItsBaseDirectory` — a `FixtureCopy` extracted with no repository → the entry's `repoRoot` null, `head.note` "root is not a repository working directory…", verdict `no_git`, **and** (for T021) the directory resolves by path; `tests/CodeMem.Tests/Support/MapStatusScenario.vb` loses its registry
- [X] T016 [US1] `src/CodeMem.Bridging/Mcp/BridgeToolDescriptions.vb` — the eight texts of contracts/tools.md §2 verbatim (the four full rewrites; the four "004's text with … replaced" edits applied exactly); dated header line. Run B09 (4) and B02's phrase fact → green
- [X] T017 [US1] Amend the 004 facts that name `projectId` or a retired kind, each Red recorded first, dated header lines: `tests/CodeMem.Tests/Bridge/B01_ReadOnlyContractTests.vb` (the `projectId 132040` argument rows of (4) removed; `solutionKey` rows kept; the hash facts unchanged); `tests/CodeMem.Tests/Bridge/B02_PortedReaderTests.vb` (the `ScopeConflict` and `RegistryAbsent` facts and the "no registry row names project N" reason fact retired — cut into `_Archive/004-store/tests/B02_RetiredFacts.vb` at T024, analyze C2; `ScopeMissing`'s new phrase "Supply solutionKey"; a `ProjectIdRemoved` fact through `BridgeHost`; the refusal-chain order fact amended to contracts/tools.md §4's sequence — arguments → configuration → map → question, no store stage — analyze G5); `tests/CodeMem.Tests/Bridge/B07_TwinPresentationTests.vb` (any `projectId` call → `solutionKey`); `tests/CodeMem.Tests/Support/FixtureMapScenario.vb`, `TwinScenario.vb`, `TypeUsagesScenario.vb`, `LiveBridge.vb` — no store path, no registry seed. `tests/CodeMem.Tests/Support/RegistryFixture.vb` is still referenced by B05 until T021: leave it
- [X] T018 [US1] `dotnet build` 0/0; run `Bridge` → B09 (1)–(5), B01, B02, B04, B07 green; B05 and B06 green (unchanged behaviour on the extract path — it still reads the store, by design until T021); `BridgeStandaloneGateTests` (1)(2) still red as named. FIRE B09 (2): remove the raw-argument check in `ScopeResolver.ValidateArguments` → `projectId` silently ignored → red; revert → green; record the Green and FIRE lines in the header of `tests/CodeMem.Tests/Bridge/B09_StandaloneTests.vb`

**Checkpoint**: the read surface and `map_status` answer by key from the map alone; `projectId` and `storePath` are refused by name; the
extract path still consults the store — Phase 4 takes it out.

---

## Phase 4: User Story 2 — A green build refreshes a mapped solution, or says how to map it (Priority: P1) 🎯 MVP (with Phase 3)

**Goal**: `extract --repo-path` and the hook resolve through `solutions.repo_root` (or the solution file's directory) and nothing else;
a directory the map does not hold gets the not-in-map answer with the exact command; the store code leaves the tree.

**Independent Test**: B10 (1)–(8) and the amended B05/B06 on fixture maps whose roots are temp directories; the hash and the launcher
seam; `BridgeStandaloneGateTests` (1)–(2) and `SqlLocationGateTests` at one file after T024.

- [X] T019 [US2] Create `tests/CodeMem.Tests/Bridge/B10_MapResolutionTests.vb` (FR-410–FR-413; Q4, Q6; STOP 1 decision 8; `<Collection("ExtractLog")>` with B05/B06) with facts (1)–(8) — every refusal asserted with 0 launches through `ScriptedLauncher` and the map's hash unchanged: (1) `ADirectoryUnderAMappedRootResolvesToItsKey` (the root itself and a subdirectory); (2) `NestedRootsResolveToTheLongest`; (3) `ASharedRootIsAmbiguous` (`AmbiguousRoot` naming both keys — two map solutions with one `repo_root`, seeded through a second extraction of the fixture under another key); (4) `ASolutionWithoutARepositoryRootResolvesAtItsBaseDirectory` (Q6 — the copy of T015's scenario, by path → its key); (5) `AnUnmappedDirectoryWithOneSlnxIsNotInMapWithTheCommand` — a temp directory holding `Fresh.slnx` → `PathNotInMap`, text contains `extract --solution-key Fresh --solution <that file's full path>` and "not in the map"; (6) `AnUnmappedDirectoryWithNoSolutionFileNamesPlaceholders` — text contains "no solution file" and `--solution-key <key> --solution <path`; (7) `AnUnmappedDirectoryWithTwoFilesIsAmbiguous` — `A.sln` and `A.slnx` → `AmbiguousSolutionFile` naming both, no `--solution-key A`; (8) `TheSuggestionNeverLooksUpOrDown` — a temp tree `parent\child` with `parent\Parent.sln` and `child` empty → `child` answers placeholders, never `Parent`. Amend `tests/CodeMem.Tests/Bridge/B05_ExtractGateTests.vb` (Reds recorded first, dated lines): the `PathNotRegistered` fact becomes the `PathNotInMap` shape, `KeyNotRegistered` becomes `SolutionKeyUnknown` with the add remedy, the `KeyUnbound`/`KeyInactive`/`MapMissingSolution` facts retired (cut into `_Archive/004-store/tests/B05_RetiredFacts.vb` at T024 — analyze C2); the gate matrix, `ExtractionRunning`, the real launch, the timeout, `stale` facts unchanged but re-seeded from map rows; `tests/CodeMem.Tests/Support/ExtractScenario.vb` loses its registry. Amend `tests/CodeMem.Tests/Bridge/B06_HookEntryTests.vb`: a payload whose command is `dotnet build C:\…\RicksLife\RicksLife.slnx -c Debug` → the target is that file's directory (FR-412); the unmapped-directory fact asserts the answer line contains `extract --solution-key`. Run → B10 (1)–(8) red (no suggestion, resolution through the registry), B05/B06 amended facts red; record
- [X] T020 [US2] Create `src/CodeMem.Bridging/Extract/SolutionFileSuggestion.vb` (R66; Q4): `Public Module`; `Inspect(directory As String) As SolutionFileSuggestion`… — a record class `src/CodeMem.Bridging/Extract/SolutionFileInspection.vb` (`Readable`, `Files As List(Of String)` (full paths, `*.sln` then `*.slnx`, ordinal order), `SuggestedKey` (one file: `Path.GetFileNameWithoutExtension`), `Command` (`"extract --solution-key " & key & " --solution " & file`, or Nothing), `Ambiguous`) and the module's `Inspect` (top level only, `Directory.Exists` false → `Readable = False`, empty lists); `Facts(inspection, path, roots)` composing the fact dictionary `BridgeRefusal.Named` needs for `PathNotInMap` (`path`, `roots`, `file`, `key`, `command`) or `AmbiguousSolutionFile` (`path`, `files`)
- [X] T021 [US2] Rewrite `src/CodeMem.Bridging/Extract/TargetResolver.vb` over map rows (R65): `Resolve(request, config, map)`; `ByKey` — `SolutionsRepository.ReadByKey` → `ResolvedTarget` (key, `LastSeenPath`, map, id) or `SolutionKeyUnknown` (with `mapPath`, `key`); `ByPath` — for every `ReadAll` row, `SolutionScope.Resolve(row.RepoRoot, Path.GetDirectoryName(row.LastSeenPath))`, `ContainsDirectory(repoPath)`, longest wins, equal lengths → `AmbiguousRoot`, none → `SolutionFileSuggestion.Inspect(repoPath)` → `PathNotInMap` or `AmbiguousSolutionFile` (roots listed, `none` when the map is empty); `src/CodeMem.Bridging/Extract/ExtractDoor.vb` `Run` — the store stage removed (`registry` gone from the call); `src/CodeMem.Bridging/Refusals/BridgeRefusalKind.vb` — remove `KeyNotRegistered`, `KeyUnbound`, `KeyInactive`, `MapMissingSolution`, `PathNotRegistered` and their texts in `BridgeRefusal.vb` (dated lines; the count is now 29 — add B09 (6) `TheVocabularyHasTwentyNineKinds`: `[Enum].GetValues(GetType(BridgeRefusalKind)).Length = 29` — analyze U1); `src/CodeMem.Bridging/Extract/HookRequest.vb` `IsSolutionOrProject` accepts `.slnx`. Build; run B10 (1)–(8), B05, B06 → green. FIRE B10 (8): make `Inspect` also look in the parent → red; revert. FIRE B10 (4): skip NULL-root rows in `ByPath` → red; revert; record both
- [X] T022 [US2] `src/CodeMem.Bridge/bridge.config.sample.json` — the three keys of contracts/cli-config-hook.md §2 (no `storePath`); `src/CodeMem.Bridge/Program.vb` untouched unless the delegates need it. Run `B05 (18)` (the shipped sample is valid JSON and loads) → green
- [X] T023 [US2] `tests/CodeMem.Tests/Support/RegistryFixture.vb` has no caller left (verify with `grep -rn RegistryFixture tests/ src/` → only its own file): remove `Support/RegistryFixture.vb` from the exclusion list in `tests/CodeMem.Tests/Guards/SqlLocationGateTests.vb` with a dated line (its literals leave the tree at T024, so the exclusion is dead); `tests/CodeMem.Tests/Bridge/B08_LiveMapTests.vb` — drop `CODEMEM_LIVE_STORE` and every `projectId`/registry expectation (Reds recorded when armed later; unarmed they skip), `map_status` armed facts expect five entries and `entries.Count = solutions.Count`; add a Skip-armed fact `ExtractByRepoPathResolvesDspProcessorFromTheMapAlone` — `LiveBridge` with `ScriptedLauncher` and a temp config with the gates on, `extract(repoPath = C:\Users\rchau\source\repos\DSP_Processor)` → `target.resolvedKey` `DSP_Processor` and the scripted request's solution path equals the map's `last_seen_path` (FR-434 on the live map, read-only — analyze G4)
- [X] T024 [US2] **The archive** (FR-402; R69; Article XIV; STOP 1 decisions 1 and 7): `git mv` to `_Archive/004-store/` keeping each file's relative name under a mirrored path — `src/CodeMem.Core/Repositories/StoreDatabase.vb`, `src/CodeMem.Core/Repositories/Registry/CodeMapSolutionsRepository.vb`, `src/CodeMem.Core/Repositories/Registry/RegistryTableMissingException.vb`, `src/CodeMem.Core/Records/RegistryRecord.vb`, `src/CodeMem.Bridging/Reading/StoreAccess.vb`, `src/CodeMem.Bridging/Reading/Envelopes/UnboundEntryEnvelope.vb`, `src/CodeMem.Bridging/Reading/Envelopes/InactiveEntryEnvelope.vb`, `tests/CodeMem.Tests/Support/RegistryFixture.vb`; write `_Archive/004-store/README.md` (what moved, from which paths, why — 152687 and v1.4.0 — the commit they came from, the seven retired refusal texts of 004 contracts/tools.md §6, and a pointer to `_Archive/004-store/tests/`, where the retired test facts move as `.vb` fragments with their headers (Article XIV; analyze C2): `BridgeSqlGateTests_RetiredFacts.vb` ((2) `RegistryReadsNameOnlyTheRegistryTable` and (4) `TheStoreIsOpenedOnlyByTheBridge`), `B02_RetiredFacts.vb` (`ScopeConflict`, `RegistryAbsent`, "no registry row"), `B04_RetiredFacts.vb` (`map_missing_solution`, unbound/inactive), `B05_RetiredFacts.vb` (`KeyUnbound`, `KeyInactive`, `MapMissingSolution`); in `tests/CodeMem.Tests/Guards/BridgeSqlGateTests.vb` cut facts (2) and (4) into their fragment with a dated header line pointing at it ((1) and (3) stay); `dotnet build` → 0/0 (nothing references the moved files — the build is the proof). Run `dotnet test --filter FullyQualifiedName~SqlLocationGateTests` → **the named Red**: `OnlyMapDatabaseOpensAConnection` expects two files, finds `{MapDatabase.vb}`; record it in the header; amend the assertion to `New String() {"MapDatabase.vb"}` with the dated line citing v1.4.0; re-run → green; FIRE (every probe below is run with `dotnet test --no-build --filter …`: the gates are source scans and the probes need not compile — analyze U2): add `Dim probe As SqliteConnection = New SqliteConnection("Data Source=:memory:")` to `src/CodeMem.Bridging/Status/MapStatusReader.vb` → red naming it; revert → green; record. Run `BridgeStandaloneGateTests` → (1) and (2) green; FIRE (1): the same probe line in `src/CodeMem.Bridging/Reading/MapAccess.vb` → red; a comment `' memos.sqlite` in `BridgeTools.vb` → red; FIRE (2): `Dim r As StoreDatabase = Nothing` in a test → red; revert each; record
- [X] T025 [US2] `dotnet test --nologo -v q` → every test green; `FileHeaderGateTests` green (archived files are outside its scan; the README needs no header); `MemOsReferenceGateTests` green; record the count under "Build and test" in the quickstart. Through the executable (CON3): `CodeMem.Bridge.exe extract --repo-path <temp unmapped dir with one .slnx> --config <fixture config>` → exit 2, the JSON's `refusal.kind` `PathNotInMap`, the command in `refusal.text`; recorded in B10 as fact (9) `TheNotInMapAnswerReachesTheCommandLine`

**Checkpoint (MVP)**: the bridge opens one file; every read tool, `map_status`, `extract --repo-path` and the hook answer from the map;
an unmapped directory gets the command that would add it; the store code is archived; the constitution and the gates agree with the
code.

---

## Phase 5: User Story 3 — The Architect adds a solution through the bridge (Priority: P2)

**Goal**: `--solution-key <name> --solution <path>` walks the same gate, door and log and adds (or re-points) a solution; `map_status`
lists every not-in-map directory the log holds until the map holds it.

**Independent Test**: B10 (10)–(15) on a copy of a fixture map with one real launch; B11 (1)–(7) over a scratch `extract.log`.

- [X] T026 [US3] Append to `tests/CodeMem.Tests/Bridge/B10_MapResolutionTests.vb` (FR-414–FR-417; Q3 as ruled; STOP 1 decision 9): (10) `AKeyWithAPathAddsTheSolution` — one real launch (`ProcessExtractorLauncher`), performed once by a class fixture `AddedSolutionFixture` shared with (11) and B11 (4) (analyze I3), with `solutionKey "Fresh"` and `solutionPath` = a `FixtureCopy`'s `Sample.sln`, on a copy of a fixture map → `exitCode` 0, `solutions` holds `Fresh` with `last_seen_path` = the path, the run's ten counts balance (observed = matched + reactivated + new; registry_before − matched − retired = 0); (11) `TheSameKeyAndPathAgainIsAReExtraction` → matched > 0, new = 0, still one `Fresh` row; (12) `AKeyAloneThatTheMapLacksIsRefusedWithTheAddRemedy` → `SolutionKeyUnknown`, text contains `--solution <path`; (13) `APathWithoutAKeyIsTargetMissing` → text names the three shapes and "solutionPath only beside solutionKey"; (14) `TheGateMatrixHoldsForTheKeyWithPathShape` — the five cells by gate name (both off; enabled only, hook origin; enabled only, tool origin → runs; onGreenBuild only, hook; both on, hook → runs) with 0 launches on refusals; (15) `OneLogLinePerCallWithTheNewTargetForm` — the target column reads `solutionKey=Fresh solution=<path>`. Create `tests/CodeMem.Tests/Bridge/B11_NotInMapTests.vb` (FR-419–FR-420; Q1 as ruled; STOP 1 decision 8; the same collection; the log beside the test executable is shared by every extract fact of the suite, so each B11 fact uses its own temp directory and asserts only on entries whose `path` is that directory — analyze A2): (1) `ARefusedDirectoryIsListedOnceWithKeyAndCommand`; (2) `TwoRefusalsForOneDirectoryAreOneEntryWithTheLaterTime`; (3) `AFreshInstanceListsTheSameDirectories` (a new `BridgeTools`/`BridgeHost` over the same log — the "restarted server"); (4) `MappingTheSolutionRemovesTheEntryAndKeepsTheLine` — over the map the shared `AddedSolutionFixture` produced (B10 (10); analyze I3), the entry for that directory is gone and the log line count unchanged; (5) `AFourOhFourEraLineIsIgnored` — append a hand-written line naming this fact's own directory with resolution `PathNotRegistered` → not listed; (6) `AnUnreadableLogIsReportedAndEntriesStand` — hold the log open with `FileShare.None` during the call → `notInMapError` set, `entries` intact; (7) `MapStatusWithTwoHundredLogLinesIsUnderThreeSeconds` (Stopwatch); (8) `StaleListsObservedDirectoriesAndNeverLaunchesForThem` — `extract(stale)` after (1)'s refusal → the stale result's `notInMap` carries the directory and the launcher seam records 0 launches for it (FR-420; analyze G1). Header: RED for the stated reasons (no `solutionPath`, no reader); record
- [X] T027 [US3] The by-key shape with a path (R67): `src/CodeMem.Bridging/Extract/ExtractRequest.vb` + `SolutionPath`; `src/CodeMem.Bridging/Extract/ExtractDoor.vb` — `RequireOneTarget`: exactly one of the three, `SolutionPath` only beside `SolutionKey` else `TargetMissing`; `TargetAsGiven` returns `solutionKey=<key> solution=<path>` when a path is given; `src/CodeMem.Bridging/Extract/TargetResolver.vb` `ByKey` — a path given → `ResolvedTarget` (key, `Path.GetFullPath(path)`, map, `SolutionId` Nothing) with no lookup (the extractor's own preflight refuses a missing file, exit 1, reported verbatim); `src/CodeMem.Bridging/Mcp/BridgeTools.vb` `Extract(context, solutionKey, solutionPath, repoPath, stale)` and `src/CodeMem.Bridge/Mcp/BridgeServer.vb`'s delegate; `src/CodeMem.Bridge/BridgeCommandLine.vb` — `--solution <path>` (extract entry only; `ExtractOnly` otherwise; alone without `--solution-key` → `ErrorText` "--solution applies only beside --solution-key"), `Usage` shows `--solution-key <key> [--solution <path>]` and the sentence "there is no --key; --solution-key is the one spelling"; `src/CodeMem.Bridge/Program.vb` passes `SolutionPath` into the request. Build; run B10 (10)–(15) → green
- [X] T028 [US3] Create `src/CodeMem.Bridging/Status/NotInMapReader.vb` (R64): `Read(mappedRoots As List(Of (Key, Root)), logPath) As (Entries As List(Of NotInMapEntryEnvelope), ErrorText As String)`; opens the log `FileShare.ReadWrite`, UTF-8; splits each line on `vbTab`; keeps lines with ≥ 4 columns whose column 4 is `PathNotInMap` or `AmbiguousSolutionFile` and column 3 begins `repoPath=`; the directory key normalised as `SolutionScope` normalises a directory (`Path.GetFullPath`, `AltDirectorySeparatorChar` → `DirectorySeparatorChar`, one trailing separator, `OrdinalIgnoreCase`) — expose that normalisation as `SolutionScope.NormalizeDirectory(text)` in `src/CodeMem.Extraction/Workspace/SolutionScope.vb` (additive, dated line) rather than restate it; one entry per key, the later line's column 1 and 2 winning; drop entries any mapped root contains (`SolutionScope.Resolve(root, root).ContainsDirectory`); `SolutionFileSuggestion.Inspect` for each survivor → `SolutionFiles`, `SuggestedKey`, `Command`, `Note` ("directory could not be read now" / "more than one solution file" / Nothing); ordered by path; any `IOException`/`UnauthorizedAccessException` → empty entries and `ErrorText`. `src/CodeMem.Bridging/Status/MapStatusReader.vb` fills `NotInMap` and `NotInMapError` last, after the repositories are asked; `src/CodeMem.Bridging/Extract/StaleResult.vb` + `NotInMap`; `ExtractDoor.RunStale` copies the status envelope's list and never launches for one. Build; run B11 (1)–(8) → green. FIRE B11 (3): filter lines by the process's start time → red; revert; record
- [X] T029 [US3] `dotnet test --filter "FullyQualifiedName~Bridge"` → green; through the executable (CON3): `CodeMem.Bridge.exe extract --solution-key Fresh --solution <fixture copy .sln> --config <copy config>` → exit 0, `run.runId` present; then `map_status` over stdio shows no `notInMap` entry for that directory; recorded in B10 (16) `AddingThroughTheCommandLineReachesTheDoor`

**Checkpoint**: a solution is added or re-pointed through the bridge by the Architect naming the key; `map_status` says which
directories still wait.

---

## Phase 6: User Story 4 — `.slnx` is an equal input (Priority: P2)

**Goal**: The extractor opens a `.slnx` end to end through its own parse; a `.sln` and a `.slnx` naming the same projects yield the same
fact set; the extension check promises exactly what the loader does.

**Independent Test**: X01 (1)–(10) against `Sample.sln`, `Sample.slnx` and hand-written `.slnx` variants in temp directories.

- [X] T030 [US4] Create `tests/CodeMem.Tests/Extraction/X01_SlnxTests.vb` (FR-421–FR-424; R61; STOP 1 decision 2; `<Collection("Fixture")>`; every extraction through `ExtractionRun.Execute` into a `TempMap`, the refusals additionally through `ExtractorProcess` for the stderr line — CON3) with ten facts: (1) `ASlnxYieldsTheSlnFactSet` — `SolutionPath` and `SolutionXPath` into two fresh maps → `MapQueries.FactSet` equal, ≥ 1 `S|`, `P|`, `E|` line; (2) `TheKeyDefaultsToTheFileNameForBoth` → `Sample` twice; (3) `LastSeenPathRecordsTheFilePassed` → ends `.sln` / `.slnx`; (4) `AMissingProjectPathIsRefusedBeforeAnyOpen` — a temp `.slnx` naming `Missing/Missing.vbproj` → exit 1, stderr contains "names a project that does not exist" and the path, the map holds no run, and the elapsed time is under 5 s (SC-406's bound — analyze I4; no build host was launched); (5) `MalformedXmlIsRefusedByName`; (6) `ANonSolutionRootIsRefusedByName`; (7) `NoProjectIsRefusedByName`; (8) `FoldersAndForwardSlashesOpen` — a `FixtureCopy` with a `.slnx` whose two projects sit inside `<Folder Name="/src/">` with `/` paths → exit 0, the same fact set as (1); (9) `AnUnsupportedExtensionIsRefusedBeforeAWorkspace` — a copy of `Sample.sln` renamed `.txt` → exit 1, "unsupported solution file", under 5 s; (10) `UsageNamesTheThreeInputs` — `CommandLine.Usage` contains `.sln`, `.slnx` and `.vbproj` on its inputs line. Run → (1) red: exit 1 "No file format header found"; (4)–(9) red; record
- [X] T031 [US4] Create `src/CodeMem.Extraction/Workspace/SlnxReader.vb` (data-model §10): `Public Module`; `ReadProjectPaths(slnxPath) As List(Of String)` — `XDocument.Load` (an `XmlException` → `WorkspaceLoadException` "solution file is not well-formed XML: <file>: <message>"), root local name `Solution` else "solution file has no Solution root: <file>", every descendant `Project` with a `Path` attribute, `/` → `Path.DirectorySeparatorChar`, `Path.GetFullPath(Path.Combine(dir, value))`, none → "solution file names no project: <file>", each checked with `File.Exists` before returning → "solution file names a project that does not exist: <path> (in <file>)". Then `src/CodeMem.Extraction/Workspace/SolutionLoader.vb` `Open` — the three-way extension door **before** `MSBuildWorkspace.Create`: `.sln` → `OpenSolutionAsync`; `.slnx` → `SlnxReader.ReadProjectPaths` then, for each path in order, skip when `workspace.CurrentSolution.Projects` already holds it (`FilePath` OrdinalIgnoreCase) else `OpenProjectAsync`; `solution = workspace.CurrentSolution`; `.vbproj` → `OpenProjectAsync`; else `WorkspaceLoadException` "unsupported solution file: <path> (expected .sln, .slnx or .vbproj)"; the XML doc of `OpenedPath` and the class say `.sln`, `.slnx` or `.vbproj`; `src/CodeMem.Extractor/CommandLine.vb` `Usage` — `--solution <path.sln|path.slnx|path.vbproj>` and the `inputs:` line of contracts/extractor.md §4. Build; run X01 → (1)–(10) green. FIRE (1): open only the first project path → the App's symbols missing → red; revert; record
- [X] T032 [US4] `dotnet test --filter "FullyQualifiedName~Extraction|FullyQualifiedName~Invariants|FullyQualifiedName~Fixpack"` → green (I02 and the 003 suites unchanged); record the run line under "Build and test" in `specs/005-bridge-stands-alone/quickstart.md`

**Checkpoint**: the `.slnx` defect of 152688 is closed on the fixture; the live re-points wait for Phase 8.

---

## Phase 7: User Story 5 — Warnings are warnings (Priority: P3)

**Goal**: A restore warning NuGet recorded is recorded on the run and the load continues; a restore error still stops it by name
before the compiler; nothing is read from the environment.

**Independent Test**: X02 (1)–(7) on the two warning fixtures and a compile-error copy.

- [X] T033 [US5] Create `tests/CodeMem.Tests/Extraction/X02_WarningTests.vb` (FR-427–FR-430; Q8 as ruled; R62, R71; STOP 1 decision 3; `<Collection("Warnings")>`; extractions through `ExtractorProcess` so the child's environment can be controlled — the launch passes an environment dictionary from which every `NoWarn*` key has been removed, asserted before the launch; (1) and (5) share one launch through a class fixture `Nu1701MapFixture`, the others launch by design — analyze I3) with seven facts: (1) `ANuGetWarningIsRecordedAndTheLoadContinues` — `Nu1701.vbproj` → exit 0, `MapQueries.ReadWarnings` ≥ 1 row with code `NU1701` and `project_path` `Nu1701.vbproj` (solution-relative to the project's own directory), the summary line ends `warnings=1`; (2) `ACompileErrorStillExitsTwo` — a `FixtureCopy` of `Nu1701` with `Broken.vb` (`Public Class Broken : Inherits Nothing`) → exit 2, `errors=` on stderr, 0 rows, no run; (3) `ARestoreErrorStopsTheLoadByNameBeforeTheCompiler` — `Nu1101.vbproj` → exit 1, stderr's first line contains `restore error NU1101` and `Nu1101.vbproj`, no `errors=` line anywhere on stderr, no run row; (4) `NoWarnInTheEnvironmentChangesNothing` — (1) again with `NoWarn=NU1701` set in the child's environment → the same exit, the same row (the variable is inert); (5) `TheBridgeReportsTheWarnings` — through `BridgeHost` on that map: `solutions` → `latestRun.warnings` = 1; `extract(solutionKey, solutionPath)` with one real launch → `run.warnings[0].code` = `NU1701`; (6) `AnUnmatchedFailureStillAborts` — a `FixtureCopy` of `Nu1701` whose `.vbproj` references `..\Nowhere\Nowhere.vbproj` → exit 1, "workspace load failed" and the MSBuild text, no `restore error`; (7) `WarningsAreWrittenBesideAFailedRunRow` — (1) with `CODEMEM_TEST_ABORT_AT`… no: with the residual seam (`RunSeams.CorruptStagedCounts` through `ExtractionRun.Execute` in-process, the I08 shape) → exit 4, the failed run row has its `NU1701` row. Run → (1) red: exit 1 "workspace load failed: Msbuild failed …"; (3) red (the text is MSBuild's, not the extractor's sentence); (5)–(7) red; record
- [X] T034 [US5] Create `src/CodeMem.Core/Records/RunWarningRecord.vb` (`Id`, `RunId`, `Code`, `ProjectPath` nullable, `Message`; here rather than at T009, behind X02's Red — analyze C1). Create `src/CodeMem.Extraction/Workspace/AssetsLog.vb` (R62): `Public Module`; `Private ReadOnly FailureShape As Regex` (module members are implicitly shared — analyze U3) = `^Msbuild failed when processing the file '(.+?)' with message: (.*)$` (Singleline); `Classify(failureMessage) As AssetsLogMatch` — parse project and text; assets path `Path.Combine(Path.GetDirectoryName(project), "obj", "project.assets.json")`; when present, `JsonDocument` → `logs[]` → the first element whose `message` equals the text (ordinal) → `AssetsLogMatch` with `Code`, `Level` (`Warning`/`Error`), `ProjectPath`, `Message`; no file, no shape, no match → `Nothing`; `src/CodeMem.Extraction/Workspace/AssetsLogMatch.vb` (the record). Then `src/CodeMem.Extraction/Workspace/SolutionLoader.vb` — gains `Public ReadOnly Property Warnings As List(Of RunWarningRecord)` and, after the opens, classifies each collected failure: `Warning` → `Warnings.Add(New RunWarningRecord With {.Code, .ProjectPath = <project full path — made relative by the run>, .Message})`; `Error` → `failures` keeps `"restore error " & code & " in " & Path.GetFileName(project) & ": " & text`; `Nothing` → the message as today; throw `WorkspaceLoadException(failures)` only when a non-warning remains. No `NoWarn` is added to the property dictionary (R62: not needed; the fact (4) proves the variable inert either way). Build; run X02 (1), (3), (4), (6) → green
- [X] T035 [US5] Create `src/CodeMem.Core/Repositories/ExtractRunWarningsRepository.vb` (`Public Module`; `InsertAll(db, runId, warnings As IEnumerable(Of RunWarningRecord))` — one parameterised INSERT per row, `project_path` bound as `DBNull` when Nothing; `ReadByRun` arrives at T036 — analyze C1); `src/CodeMem.Extraction/Run/ExtractionRun.vb` — after both `InsertCompleted` and `InsertFailed`: `ExtractRunWarningsRepository.InsertAll(db, runId, loader.Warnings)` with `ProjectPath` rewritten through `SolutionPaths.Relative(basePath, fullPath)`; the summary line: `src/CodeMem.Extraction/Run/SummaryLine.vb` `Format(…, warnings As Integer)` appends ` warnings=<n>`; both call sites pass `loader.Warnings.Count`; `src/CodeMem.Extractor/CommandLine.vb` `Usage` gains the `failure classes:` lines of contracts/extractor.md §4; `src/CodeMem.Extraction/CodeMem.Extraction.vbproj` `<Version>0.3.0</Version>` with a comment naming the two input changes. Run X02 (1), (2), (7) → green. **Named Reds** (analyze G2): `tests/CodeMem.Tests/Guards/RefusalTests.vb:78` (the anchored summary-line regex) and `tests/CodeMem.Tests/Bridge/B05_ExtractGateTests.vb:98` (the exact expected summary string) go red when `warnings=N` joins the line — record each Red, then amend with dated header lines (the regex gains ` warnings=\d+`, the expected string gains the field) → green; the tripwire sees the new INSERT under a `Repositories/` module → green
- [X] T036 [US5] The bridge side: `src/CodeMem.Bridging/Reading/Envelopes/RunWarningEnvelope.vb` (`Code`, `ProjectPath`, `Message`); `src/CodeMem.Bridging/Extract/ExtractRunEnvelope.vb` + `Warnings As List(Of RunWarningEnvelope)`; add `ReadByRun(db, runId) As List(Of RunWarningRecord)` (`SELECT id, run_id, code, project_path, message FROM extract_run_warnings WHERE run_id = @run_id ORDER BY id`) to `src/CodeMem.Core/Repositories/ExtractRunWarningsRepository.vb` — its first fact is X02 (5) (analyze C1); `ExtractDoor.ReadRun` fills it from `ReadByRun`; `src/CodeMem.Bridging/Reading/Envelopes/LatestRunEnvelope.vb` + `Warnings As Integer`; `src/CodeMem.Bridging/Reading/Readers/SolutionsReader.vb` counts `ReadByRun` per latest run inside the same read transaction. Run X02 (5) → green; FIRE X02 (3): treat every matched entry as a warning → NU1101 continues to the compiler → red (an `errors=` line or a run appears); revert; record
- [X] T037 [US5] `dotnet test --nologo -v q` → every test green; record the count under "Build and test" in `specs/005-bridge-stands-alone/quickstart.md`. The two summary-line facts amended at T035 carry their dated lines

**Checkpoint**: the defect of 152705 is closed on the fixture; RicksLife's live run waits for Phase 8.

---

## Phase 8: Polish, the document, the live steps

**Purpose**: The process document says what the code does; the live map moves to version 3 and the two `.slnx` solutions are re-pointed
at the Architect's request; the record is written; the close-out names what was left.

- [X] T038 [P] Revise `src/CodeMem.Bridge/README.md` to contracts/cli-config-hook.md §6 (the eleven sections named there; the refusal table at 29 kinds with the seven retired named as retired and a pointer to `_Archive/004-store/README.md`; the "Not in the map" section quoting the hook's line; the extractor section; the four superseded decisions named — FR-432, analyze G3); `.mcp.json` and `hooks/settings.fragment.json` untouched (assert by `git diff --stat` on both)
- [X] T039 [P] `src/CodeMem.Bridging/CodeMem.Bridging.vbproj` and `src/CodeMem.Bridge/CodeMem.Bridge.vbproj` `<Version>0.2.0</Version>`; `ProjectFileGateTests` green; `BridgeServer`'s `ServerInfo.Version` reads 0.2.0 over stdio (a one-line check in B01 (5)'s existing fact, dated)
- [ ] T040 Full suite on the Release build: `dotnet build CodeMem.sln -c Release --nologo -v q` 0/0; `dotnet test --nologo -v q` → green, under 5 minutes; `grep -rn "' FIRE:" tests/CodeMem.Tests/Bridge/B09* B10* B11* tests/CodeMem.Tests/Extraction/ tests/CodeMem.Tests/Fixpack/S03* tests/CodeMem.Tests/Guards/BridgeStandaloneGateTests.vb` → every new file carries at least one; record the run line in the quickstart
- [ ] T041 Live steps 1–3 of `specs/005-bridge-stands-alone/quickstart.md` (R73; STOP 1 decision 10): back up `C:\_DB\codemem.sqlite` to `C:\_DB\codemem.sqlite.bak-<date>-pre-005` and hash both; remove `storePath` from `bridge.config.json` beside the Release executable (gates off); `solutions` through the Release executable → `VersionBelow` naming version 2 — record it; on a **copy** of the map (a config whose `mapPath` is the copy, `extract.enabled` true): `CodeMem.Bridge.exe extract --solution-key DSP_Processor --solution C:\Users\rchau\source\repos\DSP_Processor\DSP_Processor.slnx` → exit 0, the copy at version 3, `last_seen_path` `.slnx`, the counts; `CodeMem.Bridge.exe extract --solution-key RicksLife --solution C:\Users\rchau\source\repos\RicksLife\RicksLife.slnx` with `Get-ChildItem env:NoWarn*` empty → exit 0, `warnings=3`, the three rows (OpenTK, OpenTK.GLControl, SkiaSharp.Views.WindowsForms); `solutions` and `map_status` on the copy: five entries, no registry field. Fill the Record rows 1–3
- [ ] T042 Live step 4 — **only at the Architect's request, after T041's backup**: the two extractions of T041 against `C:\_DB\codemem.sqlite` (the first upgrades it to version 3); record both summary lines, the map's hash after, and `solutions` showing both `last_seen_path` values ending in `.slnx`; arm `CODEMEM_LIVE_MAP` and run B08 → its five-entry facts green (re-pin any moved live figure with its Red recorded, as 004 did); fill Record row 4
- [ ] T043 Live step 5 — rename `C:\_DB\memos.sqlite` to `memos.sqlite.renamed-for-005` with the Shell closed; `extract --repo-path C:\Users\rchau\source\repos\DSP_Processor` (gates on) → the resolution line names `DSP_Processor` and the launch uses the `.slnx` (or the gate/`ExtractionRunning` refusal if so configured — the resolved key is what is recorded); `extract --repo-path <an unmapped repository holding one .sln, e.g. C:\Users\rchau\source\repos\vbCalc>` → `PathNotInMap` with the command; `map_status` → that directory under `notInMap`; the map's hash unchanged by the refusal; rename the store back; fill Record row 5. Nothing was added
- [ ] T044 The Operator's steps outside this repository, recorded not performed by code: delete `DSP_Processor.sln` (a commit in `C:\Users\rchau\source\repos\DSP_Processor`), re-point the two registry rows' `extraction_scope` at the `.slnx` in MemOS; then `extract --repo-path <DSP_Processor root>` once more → runs from the `.slnx`; `git -C C:\Users\rchau\source\repos\rchaudio-a11y\MemOS status --porcelain` empty before T041 and after this step; fill Record rows 6–7
- [ ] T045 Close-out: append "Implementation record (<date>)" to `specs/005-bridge-stands-alone/plan.md` (every Red/Green/FIRE by task, deviations, which of R68's two mechanisms landed, the live figures); update the spec's Status line; `specs/005-bridge-stands-alone/checklists/requirements.md` unchanged (markers are the reviewer's); the memory note for 005 revised; `git status` clean on the feature branch, the merge to `main` at the Architect's direction

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: T001 first; T002 before T003 (the gate's Red before the amendment); T004, T005, T006 in parallel after T001.
- **Foundational (Phase 2)**: after Phase 1; T007 (the S03 Red) and T008 (the named Reds) before T009; T009 → T010 → T011. **Blocks every story.**
- **US1 (Phase 3)**: after Phase 2; **T012 (the B09 Red) before T013–T017**; T013 → T014 → T015 → T016 → T017 → T018 (T015 and T016 touch different files and may overlap; T017 waits for T014's signatures).
- **US2 (Phase 4)**: after US1 (it reuses the new kinds, the key-only scope and the trimmed envelopes); **T019 (the Red battery) before T020–T021**; T020 ∥ nothing (T021 calls it); T022 ∥ T023 after T021; **T024 after T022 and T023** (nothing may reference the archived files); T025 last. Phases 3 + 4 are the MVP.
- **US3 (Phase 5)**: after US2 (it extends `TargetResolver`, `ExtractDoor` and `MapStatusReader`); T026 before T027–T028; T027 ∥ T028 (different files; both needed by T029).
- **US4 (Phase 6)**: after Phase 2 only (the loader is untouched by US1–US3); sequenced after US3 here because the suite grows in one order; T030 before T031; T032 last.
- **US5 (Phase 7)**: after US4 (both edit `SolutionLoader.vb`) and after Phase 2 (the table); T033 before T034–T036; T034 → T035 → T036 → T037.
- **Polish (Phase 8)**: T038 ∥ T039 after Phase 7; T040 after both; T041 → T042 → T043 → T044 in order, each recording before the next; T042 only at the Architect's request; T045 last.

### Within Each User Story

- The Red battery, run and recorded → the slices in the order the task list gives → Green recorded → FIREs recorded → the production-route check through the executable → the suite.

### Parallel Opportunities

- Phase 1: T004 ∥ T005 ∥ T006 (three files, no shared edits) after T001; T002 → T003 in sequence beside them.
- Phase 3: T015 ∥ T016 after T014.
- Phase 4: T022 ∥ T023 after T021.
- Phase 5: T027 ∥ T028 after T026.
- Phase 8: T038 ∥ T039.
- US4 could be built by a second hand any time after Phase 2; US5 must follow it.

---

## Parallel Example: Phase 1 after T001

```text
Task: "Create tests/CodeMem.Tests/Guards/BridgeStandaloneGateTests.vb and record its four Reds"   (T002, then T003)
Task: "Create tests/CodeMem.Tests/Fixtures/Sample/Sample.slnx and FixtureSolution.SolutionXPath"  (T004)
Task: "Create tests/CodeMem.Tests/Fixtures/Warnings/** and Support/WarningsFixture.vb"            (T005)
Task: "Add MapQueries.ReadWarnings"                                                               (T006)
```

## Parallel Example: User Story 3 after T026

```text
Task: "ExtractRequest.SolutionPath, the door's cardinality, --solution on the command line, the tool's solutionPath"   (T027)
Task: "NotInMapReader, MapStatusReader's notInMap, StaleResult.NotInMap"                                                (T028)
```

---

## Implementation Strategy

### MVP First (User Stories 1 and 2 — both P1)

1. Phase 1 and Phase 2 (the gate's Red, v1.4.0, the fixtures; schema version 3 and the pin).
2. Phase 3 (scope by key; `projectId` and `storePath` refused; `map_status` from map rows) and Phase 4 (resolution from the map, the
   not-in-map answer, the archive, the gates at one file).
3. **STOP and VALIDATE**: B09, B10 (1)–(9), B05, B06 green; `BridgeStandaloneGateTests` and `SqlLocationGateTests` green; a Claude Code
   session in `repos\CodeMem` with `memos.sqlite` absent answers every read tool and `map_status`. That is 152687's ruling delivered.

### Incremental Delivery

1. US3 — adding a solution through the bridge and `not_in_map`; the onboarding 152687 §3 asked for.
2. US4 — `.slnx`; 152688 closed on the fixture.
3. US5 — warnings; 152705 closed on the fixture.
4. Phase 8 — the document, the live upgrade and re-points at the Architect's request, the Operator's two steps, the record.

---

## Notes

- Every task that names a STOP 1 decision is revised if the Architect strikes it; the decisions are numbered in plan.md §STOP 1.
- [P] tasks = different files, no dependencies; a task that edits a file another task also edits is never [P].
- Verify every Red for its stated reason before implementing; a Red for another reason is a finding, not a Red.
- Commit after each phase at least; the amendment (T003) and the archive (T024) each on their own commit so the constitution's history reads cleanly.
- The scratch harness of research R61–R62 (`plan-research/` in the session scratchpad) is evidence, not code; nothing from it is copied into the tree except the fixture shapes T005 rebuilds from the recipe.
